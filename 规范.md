# D2H 项目规范（Specification）

> 文档状态：**草案 v0.2**　|　最优先参考：**参考项目 / d2hackmap**　|　最后更新：2026-09-19
> v0.2 修订：约束语义确认（游戏进程内存访问策略）；版本锁定 **仅 1.13c**；**展示层最后做**；确立**快照驱动开发工作流**；新增 §15 运行与入口约定（`run.bat` + `d2h/cli.py` 双日志）。

---

## 0. 阅读约定

- 本文是 D2H 项目的**纲领性规范**，先于代码编写，用于统一架构、边界与编码标准。
- 当本规范与"通用最佳实践"冲突时，**以参考项目（d2hackmap）的实际做法为准**；参考项目未覆盖之处，采用 Python 社区通用约定。
- 文中所有"参考"均指仓库内 `参考项目/d2hackmap/` 目录下的 C++ 实现与配置文件。

---

## 1. 项目定位与目标

### 1.1 背景
参考项目 `d2hackmap` 是 Sting's Hackmap for Diablo II（fixed by ding, v1.13c.1.4）的整理编译版，兼容 D2 **1.13c / 1.13d**，按同目录 `game.exe` 自适应版本号。它包含三大件：
1. `D2hackmap.exe` —— 官方战网（BN）上"安全开地图"，配套 `pluginloader.ini`、`d2hackmap.ini`、`res_sc.lng`、`remote.dll`；
2. `D2hackmap.dll` —— 单机 / 私服使用，配套 `d2hackmap.cfg`、`d2hackmap.vcb` 与全部 BMP 资源；
3. 说明文档与预设配置（`ItemInfo_*.cfg`、`*.txt` 等）。

其源码（124 个 `.cpp/.h`）按功能划分为：内存指针/结构体（`d2ptrs.h`、`d2structs.h`、`d2vars.h`）、主循环与监控（`GameMainLoop`、`GameMonitor`、`GameWindow`）、地图（`AutoMap*`、`MinimapPoint`、`RoomArea`、`RoomInfo`、`ULCPath`）、物品（`ItemDefine`、`ItemDesc`、`ItemExtInfo`、`ItemInGround`、`ItemName`、`ItemSocketProtect`、`ItemVariableProp`、`PermShowItem`、`MonsterTC`）、单位（`Monster*`、`PlayerLifeBar`、`ViewTargetUnitInfo`、`ViewUnitStat`）、配置（`LoadConfig`）、输入（`KeyEventControl`）、以及大量"增强"模块（`AutoEnchant`、`QuestProtect`、`QuickGame`、`IMEProtect`、`PacketHandler` ……）。

### 1.2 D2H 是什么
D2H 是上述参考项目的 **Python 重写 / 精神继承版**，定位为**观测型（observational）工具**：

- 从《暗黑破坏神 II》游戏进程**读取**内存，提取地图、物品、单位、玩家状态等信息；
- **不修改、不注入、不 Patch** 游戏内存（见 §2.1 硬性约束）；
- 支持对某一时刻的游戏内存状态**拍摄快照（snapshot）**，用于离线查看、回放与差异对比。

### 1.3 目标与非目标
| 目标（做） | 非目标（不做） |
|---|---|
| 只读解析 D2 进程内存 | 任何写入/改写游戏内存的行为 |
| 复用参考项目的物品代码、配色、热键等数据 | 复刻"maphack 作弊"能力（穿墙、自动施法、改属性等） |
| **仅支持 1.13c**（按 `game.exe` 指纹确认） | 支持 1.13d / 其他 D2 版本（后续可扩展） |
| 提供快照采集与离线分析 | 实时联网对战（BN）作弊规避 |

---

## 2. 核心约束（最高优先级）

### 2.1 内存访问策略 —— 只读 / 禁止写入 / 允许快照
这是本项目的**第一性约束**，高于任何功能需求。语义**已确认**：指**对游戏进程内存的访问策略**（读 / 不写 / 可拍快照），不是工具自身内存管理。

1. **只读（read-only）**：对游戏进程内存只执行读取（ReadProcessMemory 等价语义）。采集层**不得提供任何写内存的接口**。
2. **禁止写入（no-write）**：任何"写内存"的代码路径在 D2H 中**不允许存在**。参考项目中涉及写入的模块（如 `ItemSocketProtect`、`QuestProtect`、`AutoEnchant`、`QuickGame`、`NsPassBugFix`、`IMEProtect` 等）在 D2H 中**直接剔除或降级为只读提示**，绝不移植其写逻辑。
3. **允许快照（allow-snapshot）**：允许将某一时刻、选定内存区域的副本采集到本地存储，供离线解析、回放与对比。快照是"只读"的合法延伸——它复制数据而非改动源。

> 落地手段：见 §5.3 `MemoryGuard`、§9 快照系统，以及 §15 入口对权限的硬性限制（`PROCESS_VM_READ | PROCESS_QUERY_INFORMATION`，从权限层面杜绝写入）。

### 2.2 语言与运行环境
- 语言：**Python 3.11+**（以本机已配置的托管运行时为准）。
- 运行平台：**Windows**（需访问 Win32 进程内存只读 API）。
- Win32 进程内存只读访问**优先用标准库 `ctypes`**（无需额外依赖）；确需封装时再考虑 `pywin32`。版本相关偏移集中在 §5.4 偏移表中管理。

### 2.3 参考优先级
`参考项目 d2hackmap` **最优先**。具体体现在：
- 数据格式（物品代码表、配色规则、热键命名 `VK_*`）**直接复用**参考项目文件，不另起炉灶；
- 结构体字段、指针链、版本差异以参考项目 `d2structs.h` / `d2ptrs.h` 为权威来源；
- 模块划分尽量与参考项目一一对应（见 §4.2），便于溯源。

### 2.4 目标游戏版本
- 首期**仅支持 1.13c**（参考项目虽兼容 1.13c/1.13d，但 D2H 锁定 1.13c）。
- 进程名：`game.exe`（D2 主进程）。

---

## 3. 设计原则

1. **观测不介入**：工具的存在不得改变游戏运行时状态。一切功能以"能读出来"为前提。
2. **快照可重放**：任何一次内存解析都能在"实时"与"快照"两种数据源上**无差别运行**，靠统一的 `Reader` 接口屏蔽差异。
3. **配置可移植**：尽量沿用参考项目既有的 `.cfg` / `.ini` 文本格式，降低迁移成本，老用户可直接复用。
4. **模块化、可测试**：采集 / 解析 / 配置 / 展示 / 快照 分层解耦；解析逻辑不依赖真实进程，可用快照做单元测试。
5. **安全边界硬校验**：`MemoryGuard` 在 API 层面物理阻断写内存；CI / 启动自检扫描代码中是否出现写内存调用。
6. **离线优先（快照驱动）**：开发期先拍一次快照，此后绝大部分开发（偏移校准、结构体解析、配置加载、单元测试）基于本地快照离线进行，**无需游戏常驻**。
7. ★★**禁止推测游戏内状态（2026-09-20 用户定死）**：工具**只负责把读到的原始值完整呈现**，
   不代替用户判断"游戏现在处于什么状态"。

   - **反面案例（都真实发生过）**：
     · 离线读到 `0x11C2F4 == 1` 就断定"那是标志位不是指针" ⇒ 实机其实是另一回事，结论反复横跳；
     · 凭"UI 打开时鼠标应该在界面上"就加阻断规则 ⇒ 把开着背包悬停 NPC / 地面物品**全部误杀**；
     · 拿"我认为地面物品应该走哪条链"去裁剪输出 ⇒ 明明读到了却被藏住，反被当成地址错误。
   - **正确做法**：
     ① 拿不到实机读数时，**如实说"未验证"**，不要把推测写成结论；
     ② 所有判定必须在输出里**标注依据**（如 `HoverFlag=0`、`nLocation==0`），让用户一眼能对照校验；
     ③ 派生结论与原始值**分栏打印**，推断部分明确标为"程序推断，待校验"；
     ④ 状态语义的最终解释权在**用户实机校验**，他反馈后我再改代码，不预先替他下判断。
   - 落地：`cli._print_hover_frame()` 等输出函数的"判定"段一律带依据标注。

---

## 4. 系统架构

### 4.1 分层
```
┌─────────────────────────────────────────────┐
│                 展示层 UI  (最后做, M6)        │  叠层 / 小地图 / 控制台（只读显示）
├─────────────────────────────────────────────┤
│                 配置层 Config                  │  物品配色 / 热键 / 版本偏移 / 资源
├─────────────────────────────────────────────┤
│    解析层 Parse   │   快照层 Snapshot          │  物品/单位/地图/玩家 ｜ 采集/存储/回放
├─────────────────────────────────────────────┤
│             采集层 Acquire (只读)              │  Reader 接口 → MemoryGuard → RPM(ctypes)
└─────────────────────────────────────────────┘
                         │
                ┌────────▼────────┐
                │  D2 进程内存      │  ← 仅读取，绝不写入
                └─────────────────┘
```

### 4.2 模块清单（对照参考项目）
| 层 | D2H 模块（Python 包） | 对应参考项目 |
|---|---|---|
| 采集 | `acquire.process` / `acquire.memory` / `acquire.snapshot` | `Environment`、`D2CallStub`、`GameMonitor` |
| 采集 | `acquire.offsets` / `acquire.structs` | `d2ptrs.h`、`d2structs.h`、`d2vars.h`、`MyStructs.h` |
| 解析 | `parse.items` | `ItemDefine/Desc/ExtInfo/InGround/Name/VariableProp`、`MonsterTC` |
| 解析 | `parse.units` | `Monster*`、`PlayerLifeBar`、`ViewTargetUnitInfo`、`ViewUnitStat` |
| 解析 | `parse.map` | `AutoMap*`、`MinimapPoint`、`RoomArea`、`RoomInfo`、`ULCPath` |
| 解析 | `parse.player` | `ShowOrbs`、`PlayerLifeBar`、`GameCount` |
| 配置 | `config.loader` / `config.item_colors` / `config.hotkeys` | `LoadConfig`、`物品配置说明.txt`、`快捷键说明.txt` |
| 配置 | `config.data`（捆绑资源） | `d2hackmap.vcb`、`ItemInfo_*.cfg`、`*.bmp`、`res_sc.lng` |
| 展示 | `ui.overlay` / `ui.minimap` / `ui.console` | `ScreenHook`、`m_DrawFunc`、`GameChat`、`MessageLog` |
| 快照 | `snapshot.store` / `snapshot.replay` / `snapshot.diff` | （新增，参考项目无） |
| 入口 | `d2h/cli.py`（由 `run.bat` 唤起） | `DllMain`、`GameMainLoop` |

> **明确剔除**（因 §2.1 禁止写入）：`ItemSocketProtect`、`QuestProtect`、`AutoEnchant`、`QuickGame`、`QuickBackTown`、`QuickSwapItem`、`NsPassBugFix`、`IMEProtect`、`PacketHandler`、`PermShowItem`（其"永久显示"原靠写内存实现）、`GameFilter` 的拦截写部分。其中部分可改造为"只读提示"（如检测到某状态仅在 UI 标注，不改动游戏）。

---

## 5. 采集层（Acquire，只读）

### 5.1 进程附加
- 按进程名 `game.exe` 枚举并打开进程，**仅申请 `PROCESS_VM_READ` + `PROCESS_QUERY_INFORMATION`** 权限，从权限层面杜绝写入。
- 版本识别：读取同目录 `game.exe` 特征（版本号 / 时间戳）确认 **1.13c**。

### 5.2 内存读取原语
- 封装 `read_bytes(addr, size)`、`read_int` / `read_uint` / `read_float` / `read_struct(addr, StructType)`，基于标准库 `ctypes` 调 `ReadProcessMemory`，**不引入写接口**。
- 所有读取走统一 `Reader` 抽象；真实进程与快照实现同一接口（见 §9）。
- 读取越界 / 进程退出时返回 `None` 并记日志，**不抛未捕获异常**中断主循环。

### 5.3 MemoryGuard（硬性屏障）
- 单例，对外**只暴露读方法，不暴露任何写方法**。
- 启动时静态扫描：若仓库内出现 `WriteProcessMemory` / `write_*` / `poke` 等写内存符号，构建/自检失败。
- 这是 §2.1 的"物理"落地，而非仅靠约定。

### 5.4 偏移 / 指针表（版本自适应）
- `acquire/offsets.py`：以 dict 形式存 `{version: {symbol: offset/path}}`，对应参考 `d2ptrs.h`。首期只填 `1.13c`。
- 指针链解析（`GameData → UnitList → Unit → ...`）对应 `d2structs.h`，用 `@dataclass` 描述结构体，字段含偏移、类型、是否指针。

### 5.5 结构体定义
- `acquire/structs.py`：用 Python `dataclass` + 类型注解描述 D2 关键结构（Unit、ItemData、Room、Level、Player 等），与参考 `d2structs.h` 字段对齐，便于对照维护。

---

## 6. 解析层（Parse）

- **`parse.items`**：解析地面 / 背包 / 装备物品。复用参考 `物品代码.txt`（来自 `.vcb`，约 40KB 物品代码表）做代码→名称映射；解析物品属性（`ItemVariableProp`）、孔数、品质（破碎/普通/超强/魔法/套装/稀有/独有/橙色）、无形否。
- **`parse.units`**：解析怪物 / 玩家 / NPC 单位节点链，提取坐标、生命、名称。
- **`parse.map`**：解析房间（`Room`）、关卡（`Level`）、小地图单元格（`AutoMapCell` / `AutoMapBlob`），重建可显示地图。
- **`parse.player`**：解析玩家状态、法力 / 血球（`ShowOrbs`）、计数（`GameCount`）。
- 所有解析函数签名为 `parse_xxx(reader: Reader, base_addr: int) -> Model`，因此**实时与快照共用一套代码**。

---

## 7. 配置层（Config）

### 7.1 复用参考格式
- **物品代码表**：直接搬运 `d2hackmap.vcb`（格式：`全称, 代码: 序号`），解析为 `{code: name}` 与 `{name: code}`。
- **物品配色规则**：解析 `d2hackmap.cfg` 中的 `Item Colours[物品代码][品质][无形][孔数]` 与 `Item Hide Lvl[...]`（语法见参考 `物品配置说明.txt`）。配色值支持 `-1`（默认）、`-2`（隐藏可切换）、代码值；小地图色用 `0x[横][竖]`。
- **热键**：沿用参考 `VK_*` 命名（`VK_NUMPAD0`、`VK_F1` …），定义"显示模式切换 / 快照采集 / 叠层开关"等绑定。
- **语言包**：复用 `res_sc.lng`（中文界面）与既有 `*.txt` 说明。

### 7.2 配置加载
- `config/loader.py`：以"宽松行解析"读取 `.cfg` / `.ini`，兼容参考项目既有注释（`//`）与中文字符（UTF-8 / GBK 自适应）。
- 默认配置随包分发；用户配置覆盖默认。

---

## 8. 展示层（UI，只读显示，最后做）

- **`ui.overlay`**：屏幕叠层（对应参考 `ScreenHook` / `m_DrawFunc`），仅把解析结果画出来，**不回写游戏**。
- **`ui.minimap`**：小地图点（`MinimapPoint`）渲染，按 `Item Colours` 的模式 1/2/3 切换显隐。
- **`ui.console`**：命令行 / 日志面板（`MessageLog`、`GameChat` 的信息展示部分）。
- 展示层完全被动，数据源可是实时 `Reader` 或快照 `Reader`，无差别。
- **本层排在最后（M6）实现**；在此之前所有结果以控制台 / 日志 / 快照文件呈现。

---

## 9. 快照系统（Snapshot，允许）

这是 D2H 相对参考项目**新增**的核心能力，也是 §2.1"允许快照"的落地：

1. **采集（capture）**：在某一时刻，按 §5.4 偏移表选定的关键区域，调用只读 `Reader` 把字节副本存入本地。**全程只读**。
2. **存储（store）**：快照含元数据（`meta.json`：版本、时间戳、进程指纹、区域清单）与数据块（`game_module.bin` 等）；默认以"目录 + 文件"存储，后续可换 SQLite（M2 细化）。
3. **回放（replay）**：`snapshot.replay` 把快照封装成与实时同构的 `Reader` 实现，解析层无感知地跑在历史上。
4. **差异（diff）**：对比两次快照（如物品增减、地图变化），辅助分析。
5. **安全**：快照本身是本地只读副本，绝不回写游戏进程。
6. **快照驱动开发工作流**：开发期先对运行中的游戏拍一次快照（`d2h/cli.py snap`），之后**绝大部分开发（偏移校准、结构体解析、配置加载、单元测试）都基于本地快照离线进行，无需游戏常驻**。实时 `Reader` 与快照 `Reader` 同构，因此同一套解析代码既能在游戏运行时跑，也能在快照上跑。仅"验证真实内存布局""确认指针链""发布前回归"才需要游戏在线。

---

## 10. 目录结构（建议）

```
D2H/
├── 规范.md                  # 本文件
├── pyproject.toml
├── run.bat                  # 纯 ASCII 启动器，双击唤起 d2h/cli.py
├── logs/                    # 每次运行的 txt 日志（控制台输出副本）
├── snapshots/               # 快照存储（capture 产出）
├── d2h/
│   ├── cli.py               # 统一入口（所有 py 经此执行）
│   ├── acquire/             # 采集层（只读）
│   │   ├── process.py
│   │   ├── memory.py
│   │   ├── offsets.py
│   │   ├── structs.py
│   │   └── guard.py         # MemoryGuard
│   ├── parse/               # 解析层
│   │   ├── items.py
│   │   ├── units.py
│   │   ├── map.py
│   │   └── player.py
│   ├── config/              # 配置层
│   │   ├── loader.py
│   │   ├── item_colors.py
│   │   ├── hotkeys.py
│   │   └── data/            # 捆绑参考资源（vcb/cfg/lng/bmp）
│   ├── ui/                  # 展示层（最后做）
│   │   ├── overlay.py
│   │   ├── minimap.py
│   │   └── console.py
│   └── snapshot/            # 快照层
│       ├── store.py
│       ├── replay.py
│       └── diff.py
└── tests/                   # 基于快照的单元测试
```

---

## 11. 数据格式与资源（直接复用参考项目）

| 资源 | 来源文件 | 用途 |
|---|---|---|
| 物品代码表 | `参考项目/.../d2hackmap.vcb`（含 `物品代码.txt`） | 物品 code↔名称映射 |
| 物品配色 | `d2hackmap.cfg` / `ItemInfo_Chi.cfg` / `ItemInfo_Eng.cfg` | 颜色与隐藏规则 |
| 热键 | `快捷键说明.txt` + `d2hackmap.ini` | 键位定义 |
| 语言 | `res_sc.lng` | 中文界面 |
| 图标 | `*.bmp`（68 个） | 小地图 / 物品图标 |
| 版本偏移 | `d2ptrs.h` / `d2structs.h` | 指针链与结构体（仅 1.13c） |

> 这些文件**原样搬入 `config/data/`**，不做破坏性改写，保持与参考项目一致以便溯源与升级。

---

## 12. 编码规范（Python）

- **风格**：遵循 PEP 8；行宽 100；类型注解强制（`from __future__ import annotations`）。
- **命名**：包/模块 `snake_case`；类 `PascalCase`；常量 `UPPER_SNAKE`；与参考项目同名概念（如 `Room`、`Unit`、`ItemData`）保留原词以便对照。
- **日志**：统一 `logging`，分级；采集失败记 `WARNING` 不中断。每次运行**控制台 + `logs/*.txt` 双写**（见 §15）。
- **错误**：解析/读取失败返回哨兵值（`None` / `Empty`）而非抛异常穿透主循环；顶层 `cli` 捕获并友好提示。
- **测试**：`tests/` 以快照为 fixtures，覆盖解析层；CI 必须跑；同时跑 §5.3 写内存符号扫描。
- **依赖**：最小化；优先标准库（`ctypes` 做 Win32 只读内存访问），不轻易引入第三方包；若必须，在隔离 venv 内安装，不污染本机。
- **中文**：源码注释、文档、用户可见文案用简体中文；配置文件内部键名沿用参考英文（兼容老格式）。
- ★★**输出必须完整（2026-09-20 定死）**：任何「读对象 / 读结构」的命令，**所有字段一律打印，空值显式打 `-`**，
  禁止因为「当前判定用不上 / 觉得没意义」而裁剪或按条件隐藏。
  - **根因**：`hover` 曾把悬停文本只放在 `HoverFlag=1` 的分支里打印 —— 明明读到了 `融解药`，
    却因为开关是 0 被藏住，反被误判成「地址错误」、白查一轮。**不显示 ≠ 没读到**。
  - 要求：
    ① 原始观测量（开关 / 坐标 / 文本缓冲 / 全局值）**不受任何判定门控影响，永远打**；
    ② 缺失字段打 `-`，让人一眼分清「读到空」和「根本没读」；
    ③ 指针统一 8 位十六进制补零（便于肉眼比对）；
    ④ 派生判定单独成段（如 `---- 判定 ----`），与原始量分开，不要混在一起。
  - 轮询类命令（`/ --watch`）可以「仅变化时打印」以省眼，但**打印的那一帧必须是完整结构**
    （`--raw` = 每帧都打）。
  - 参考实现：`cli._print_hover_frame()`。

---

## 13. 开发里程碑

| 阶段 | 内容 | 退出标准 |
|---|---|---|
| M0 骨架与入口 | 目录结构、`pyproject`、`run.bat`、`d2h/cli.py` 双日志（控制台+txt）、`MemoryGuard` 写内存静态扫描 | 入口可跑 `info`；扫描通过 |
| M1 采集层 | `acquire.process/offsets/structs/game`：只读打开 `D2loader.exe`，按 1.13c 偏移读出模块基址、`InGame`/`GameInfo`/`PlayerUnit`、玩家名/坐标、背包物品枚举 | 对真实游戏验证：能打印 InGame/角色名/坐标/物品（✅ 已完成，提交 `763a69c`） |
| M2 快照系统 | `snapshot.store/replay/diff`：采集+离线回放+差异；确立"快照驱动开发"闭环 | 离线回放与实时读取同构 |
| M3 配置层 | `config.loader` 完整解析 `.cfg/.ini/.vcb/.lng`，物品代码/配色/热键入库 | 老配置可直接载入 |
| M4 解析-物品 | `parse.items` + 物品代码表 + 配色规则 | 快照单测：地面物品解析正确 |
| M5 解析-地图/单位/玩家 | `parse.map/units/player` | 房间/单位/玩家状态可解析 |
| M6 展示层（最后做） | `ui.overlay/minimap/console` | 实时/快照叠层显示物品与单位 |

---

## 14. 功能边界（因禁止写入而剔除 / 改造）

下表明确"参考项目有、D2H 不做"的能力，作为合规红线：

| 参考模块 | 原行为 | D2H 处置 |
|---|---|---|
| `ItemSocketProtect` | 写内存保护孔位 | 剔除 |
| `QuestProtect` | 改写任务状态 | 剔除 |
| `AutoEnchant` | 自动附魔（写） | 剔除 |
| `QuickGame` / `QuickBackTown` / `QuickSwapItem` | 改游戏行为 | 剔除 |
| `NsPassBugFix` / `IMEProtect` | Patch 修复 | 剔除 |
| `PacketHandler` | 收发/篡改封包 | 剔除（仅保留只读日志展示） |
| `PermShowItem` | 写内存永久显示 | 改造为"UI 标注"，不写内存 |
| `GameFilter` 拦截写 | 拦截/改写 | 改造为只读提示 |

> 任何新需求若需要"写游戏内存"才能达成，**一律拒绝**，并在本表追加记录。

---

## 15. 运行与入口约定

- **统一入口**：所有 D2H 的 Python 执行都经由 `d2h/cli.py`，不再单独 `python xxx.py`。入口负责：解析子命令、初始化双日志、统一异常出口。**后续各模块能力均以子命令挂载到本入口**。
- **启动器 `run.bat`**：**必须纯 ASCII**（cmd 按本地代码页解析 bat，UTF-8 中文会把整个批处理解碎导致双击闪退）。它只做两件事：带参数则透传给入口；无参数则唤起 `cli.py menu`。
- **中文界面一律由 Python 渲染**：菜单、提示、交互全部在 `cli.py menu` 里用 `print()` + `input()` 完成（同一进程内复用子命令，无需反复启动 Python）。bat 里不写任何中文，从根上规避编码问题。
- **编码环境**：不设 `PYTHONUTF8`/`PYTHONIOENCODING`，让 Python 按控制台代码页（cp936）输出中文，双击运行时中文正常；日志 `FileHandler` 显式 `encoding="utf-8"`，落盘始终 UTF-8。
- **双日志**：每次运行同时输出到
  1. 控制台（stdout，bat 可见，即"向 bat 输出执行过程"）；
  2. `logs/d2h_<时间戳>.txt`（落盘存档，便于回溯）。
  日志含级别、模块、时间；采集失败记 `WARNING` 不中断。
- **子命令**（随阶段扩展）：`info`（项目/约束/版本）、`menu`（**交互式中文菜单**，bat 无参数默认进入）、`snap`（对游戏拍快照，游戏不在则友好提示）、`list`（列快照）、`parse <快照名>`（离线解析摘要）、`tmp`（查看/清理临时目录）。
- **离线优先**：无游戏时 `info / list / parse` 照常工作；仅 `snap` 需要游戏在线。
- **发键仅投递窗口消息**：开发期需要切换 UI 时用 `d2h/tools/keys.py`（`PostMessage` 投递 `WM_KEYDOWN/WM_KEYUP`）。**这不属于写内存**：不改进程内存、不注入、不 Patch。对应子命令 `send <键>`（i/q/c/t/esc）。

### 15.1 已验证的只读观测点（1.13c，实测自真实游戏）

> 所有地址均为**多级指针**，必须逐层解引用；基址一律取进程**真实模块基址**（D2Game/D2Lang 等会重定位）。

**A. 游戏状态码（在/不在游戏）** —— `*( *( Fog.dll + 0x4AFE0 ) + 0x08 )`，4 字节 DWORD

| 值 | 含义 |
|---|---|
| 11 | 战网登录界面 |
| 14 | 人物选择界面 |
| 2xxx | 战网(BN) 游戏内 |
| 3xxx | 单机游戏内 |
| 4096 | 实测出现过，确在游戏内（未归类细分） |

**B. 游戏内 UI 标志数组（UIVar）** —— `p = *( D2CLIENT.dll + 0x50D00 )`（**不解引用会读到九位数垃圾**）；
**数组起点 = `p - 4`**（默认基址下 `p = 0x6FBAAD84`，数组起点 `0x6FBAAD80`）。每项 4 字节 DWORD，
`偏移 = index × 4`，**共 38 项（index 0..37），占 0x6FBAAD80 ~ 0x6FBAAE14**。

> ⚠️ 早期把 `p` 当数组起点，导致全部偏移少 4（整体错位一格）。2026-09-19 与参考项目
> `d2hackmap/d2vars.h` 的 `enum UIVar` 逐项对齐后修正。**边界硬证据**：index 0 = 1（hackmap 注 `always 1`）、
> index 19 = 1（hackmap 注 `init 1`）、数组前 8 个 DWORD 全 0、index 38 起是 `0x02880223` 这类指针值（越界）；
> 且 index 10 = `0x6FBAADA8` = `d2ptrs.h` 点名的全局变量 `AutomapOn`。
> 周边扫描（`arr-0x100 ~ arr+0x200`）：数组外非 0 的只有 `0x6FBAAD50/0x6FBAAD58` 与
> `0x6FBAAE1C/0x6FBAAE24`（均为 `0x0288xxxx` / `0x16DD4000` 指针），**不存在第二块 0/1 开关数组**。

| idx | 偏移 | 绝对地址 | 面板 | hackmap UIVar | 键 | 状态 |
|---|---|---|---|---|---|---|
| 0 | `+0x00` | `0x6FBAAD80` | 数组头（恒 1） | UNK0 | - | 已验证（=1） |
| 1 | `+0x04` | `0x6FBAAD84` | 背包 | INVENTORY | I | 已验证 |
| 2 | `+0x08` | `0x6FBAAD88` | 属性 | STATS | C | 已验证 |
| 3 | `+0x0C` | `0x6FBAAD8C` | 技能组（左右手技能选择，**不是技能树**） | CURRSKILL | - | 用户已核验 |
| 4 | `+0x10` | `0x6FBAAD90` | 技能树 | SKILLS | T | 已验证 |
| 5 | `+0x14` | `0x6FBAAD94` | 聊天输入 | CHATINPUT | ENTER | 用户已核验 |
| 6 | `+0x18` | `0x6FBAAD98` | 新属性点按钮（当前=1） | NEWSTATS | - | 用户已核验 |
| 7 | `+0x1C` | `0x6FBAAD9C` | 新技能点按钮（当前=1） | NEWSKILL | - | 用户已核验 |
| 8 | `+0x20` | `0x6FBAADA0` | NPC 对话框 | INTERACT | - | 用户已核验 |
| 9 | `+0x24` | `0x6FBAADA4` | 设置 | GAMEMENU | ESC | 已验证 |
| 10 | `+0x28` | `0x6FBAADA8` | 地图（= 全局 `AutomapOn`） | AUTOMAP | TAB | 用户已核验 |
| 11 | `+0x2C` | `0x6FBAADAC` | 配置快捷键 | CFGCTRLS | - | 用户已核验 |
| 12 | `+0x30` | `0x6FBAADB0` | 商店（NPC 交易/修理） | NPCTRADE | - | 用户已核验 |
| 13 | `+0x34` | `0x6FBAADB4` | 显示地面物品 | SHOWITEMS | ALT | 用户已核验 |
| 14 | `+0x38` | `0x6FBAADB8` | 打孔 / 注入窗（原称"任务物品提交窗"） | MODITEM | - | 用户已核验 |
| 15 | `+0x3C` | `0x6FBAADBC` | 任务 | QUEST | Q | 已验证 |
| 16 | `+0x40` | `0x6FBAADC0` | UNK16 | UNK16 | - | 未实测 |
| 17 | `+0x44` | `0x6FBAADC4` | 任务日志按钮（左下角） | NEWQUEST | - | 用户已核验 |
| 18 | `+0x48` | `0x6FBAADC8` | 下部面板（置位时不重绘） | STATUSAREA | - | 未实测 |
| 19 | `+0x4C` | `0x6FBAADCC` | UNK19（初值 1） | UNK19 | - | 已验证（=1） |
| 20 | `+0x50` | `0x6FBAADD0` | 传送 | WAYPOINT | - | 用户已核验 |
| 21 | `+0x54` | `0x6FBAADD4` | 迷你标签栏 | MINIPANEL | - | 用户已核验 |
| 22 | `+0x58` | `0x6FBAADD8` | 组队 | PARTY | P | 已验证 |
| 23 | `+0x5C` | `0x6FBAADDC` | 玩家交易 | PPLTRADE | - | 用户已核验 |
| 24 | `+0x60` | `0x6FBAADE0` | 信息页（消息日志） | MSGLOG | - | 用户已核验 |
| 25 | `+0x64` | `0x6FBAADE4` | 仓库 | STASH | - | 已验证 |
| 26 | `+0x68` | `0x6FBAADE8` | 盒子 | CUBE | - | 用户已核验 |
| 27 | `+0x6C` | `0x6FBAADEC` | UNK27 | UNK27 | - | 未实测 |
| 28 | `+0x70` | `0x6FBAADF0` | 背包2 | INVENTORY2 | - | 未实测 |
| 29 | `+0x74` | `0x6FBAADF4` | 背包3 | INVENTORY3 | - | 未实测 |
| 30 | `+0x78` | `0x6FBAADF8` | 背包4 | INVENTORY4 | - | 未实测 |
| 31 | `+0x7C` | `0x6FBAADFC` | 腰带 | BELT | - | 未实测 |
| 32 | `+0x80` | `0x6FBAAE00` | UNK32 | UNK32 | - | 未实测 |
| 33 | `+0x84` | `0x6FBAAE04` | 帮助 | HELP | H | 用户已核验 |
| 34 | `+0x88` | `0x6FBAAE08` | UNK34 | UNK34 | - | 未实测 |
| 35 | `+0x8C` | `0x6FBAAE0C` | 玩家头像列表（当前=1） | PARTYHEAD | - | 用户已核验 |
| 36 | `+0x90` | `0x6FBAAE10` | 佣兵装备 | PET | O | 已验证 |
| 37 | `+0x94` | `0x6FBAAE14` | 任务卷轴（点任务物品时显示） | QUESTSCROLL | - | 用户已核验 |

**C. 与 B 同源的聚合位（实测联动）**

| 地址 | 语义 | 备注 |
|---|---|---|
| `D2CLIENT.dll + 0x11C414` | 0=无 / 1=右开 / 2=左开 / 3=左右开 | 与 B 联动：背包·技能树→1，属性·任务→2，两者同开→3；设置不计入 |
| `D2CLIENT.dll + 0x11BC34` | 12=仓库 / 14=盒子 | 12 已实测（开仓库时=12）；**14 待你开盒子复核**（盒子需鼠标右键开启，无按键可触发，故留给 `uiwatch.bat` 实操验证） |

> 验证方式：`uiwatch.bat` 双击监听 → 手动开盒子 → ESC（会退到「仓库+背包」），窗口会逐行打印面板与聚合位变化。

**D. 鼠标指向的单位** —— 主观测点 `D2CLIENT.dll + 0x11BC38`（绝对 `0x6FBCBC38`），`UnitAny*`

- **来源（权威）**：参考项目 `d2ptrs.h:139`
  `D2VARPTR2(D2CLIENT, 0x6FBCBC38, ..., CurrentViewItem, UnitAny*)  // 选择显示的物品`。
  这是 hackmap 里**唯一**一个非 `PlayerUnit` 的 `UnitAny*` 全局，即"当前查看/悬停的对象"。
- 观测：`cli.py hover`（菜单 15）/ `hover --watch`（菜单 16，0.15 秒轮询，变化才打印），
  解出 `dwUnitType / dwTxtFileNo / dwUnitId / mode / 坐标`，玩家还显示角色名。
  类型：0 玩家 / 1 怪物·NPC / 2 物件 / 3 导弹 / 4 物品 / 5 房间格。
- ⚠️ **`D2CLIENT + 0x11C2F4` / `+0x11C2F8` 已拍板为标记（不是指针）**（2026-09-20 老大实机）：
  悬停各种东西时它们始终只有 0/1，且**只对地面物体本体响应，不对地面物品名文本框响应**
  （早期误当指针解引用 ⇒ 崩溃）。现仅作诊断打印，代码里 `_try_ptr()` 校验会自然跳过。
  反汇编（`GetSelectedUnit` `0x6FB01A80`）两种读法都成立，故不再纠结语义：
  ```
  6FB01A80  A1 F4 C2 BC 6F      mov eax,[0x6FBCC2F4]
            85 C0 / 74 53       test eax,eax ; je -> return 0
            8B 15 4C 96 BC 6F   mov edx,[0x6FBC964C]   ; hover_type
            8B 0D 38 96 BC 6F   mov ecx,[0x6FBC9638]   ; hover_id
            C1 E2 09 / 83 E0 7F shl edx,9 ; and eax,0x7F
            81 C2 08 A6 BB 6F   add edx,0x6FBBA608     ; unit 表
            E8 76 33 05 00      call 查桶               ; 命中 -> esi
            8B C6 / 5E / C3     return esi
  6FB01AC4  C7 05 F8 C2 BC 6F 0 / C7 05 F4 C2 BC 6F 0  ; 查不到时把两个都清 0
  ```
  离线实测（鼠标没悬停）两个值都是 `1`；`+0x11C300/0x11C304` 才是指针但指向
  `type=6 / unitId=0xEEEEEEEE`（D2 的**已释放**标记），**不是悬停单位**。
  ★**"地面物品名文本框"另有解法** —— 见本段下方的 `D2WIN+0xC9E58` 悬停文本。
- **已实机确认（2026-09-20）**：悬停 NPC 走 `(hover_id, hover_type)` 反查链路（实测卡夏/瓦瑞夫/恰西均正确），
  悬停 UI 内物品走 `CurrentViewItem`（仓库里在宝石间移动，读数精确跟随）。两者用途不同，见下条 H。
- ⚠️**坐标必须按类型分路径结构读**（2026-09-20 修正；曾对物件读出 `y=65535`）：
  `UnitAny.pPath` 是 union —— 玩家/怪物/导弹用 **DynamicPath**（WORD @`+0x02`/`+0x06`），
  物件/物品/地块用 **StaticPath**（DWORD @`+0x0C`/`+0x10`）。依据参考项目 `d2structs.h`。
  统一入口 `game.read_unit_pos(handle, p_path, unit_type)`，**所有读坐标处都走它**，不要再手写偏移。
- **单位哈希表**（悬停只有 `(unitId, 类型)`，必须靠它反查成 `UnitAny*`）：
  `D2CLIENT + 0x10A608`（绝对 `0x6FBBA608`），按类型分块（块索引 = 类型，每块 512 字节 = 128 桶），
  桶 = `unitId & 0x7F`，桶内沿 `pListNext(+0xE8)` 比对 `(dwUnitType, dwUnitId)`。
  实现 `game.find_unit_by_id()`；自检 `find_unit_by_id(1, 0)` 返回的正是 `PlayerUnit`。
- ✅**已实机确认（2026-09-20）**：物件（储藏箱 `type=2 txt=267 id=0x15`）与玩家均可正确解析，
  坐标按 StaticPath/DynamicPath 分流后正常。NPC/怪物（`type=1`）同走 `(unitId, 类型)` 反查路径，同源可靠。

**E. 游戏字符串表（D2Lang）** —— 物品/技能/任务/对白等所有显示文本的**唯一权威来源**

- **必须查内存，不能查本地文件**（2026-09-20 用户强调）：`.tbl` 是运行时载入堆的，
  且服务端/mods 会改写内容，本地文件既不全也不准。实测字符串落在 `0x11EFxxxx`、`0x03F9805C`
  这类**堆地址**，不在 D2Lang 镜像里。
- **来源**：hackmap 走 `D2GetItemTxt(txtFileNo)->wLocaleTxtNo` 再 `D2GetLocaleText(no)`
  （`d2ptrs.h:230`，`D2LANG + 0x9450`）。那是**函数调用**，只读约束下不能调，
  于是**只读 dump 该函数体**把表结构扒出来自己算（与 `items.py` 取 ItemTxt 同一手法）。
  实现：`d2h/acquire/lang.py` → `LocaleText.get(id)`。
- **结构**（1.13c，`D2LANG + 0x9450` 反汇编）：
  ```
  id >= 20000 -> 表 D2LANG+0x10A84，基准 20000
  id >= 10000 -> 表 D2LANG+0x10A80，基准 10000
  否则        -> 表 D2LANG+0x10A64，基准 0
  count = WORD[table + 0x02]
  slot  = WORD[table + 0x15 + (id - 基准) * 2]     ← id -> 槽位映射
  str   = wchar_t* at DWORD[strings + slot * 4]    ← strings = +0x10A68 / +0x10A6C / +0x10A70
  ```
  实测规模：base 5427 条 / mid 1169 条 / high 2991 条。
- **颜色控制符**：原文含 `ÿcX`（如 `ÿc2◇ ÿc/裂开红ÿc/`），显示前用 `lang.strip_color()` 清洗。
  ⚠️ 多行文本（`\n`）、带 `[]` 的文本**原样保留**，不要额外加工。
- **查询**：`cli.py lang <id>...` / `--range LO HI` / `--search 关键字` / `--raw`（菜单 20）。
  实测：`lang 2039` → `短棍【普通】`；`lang 10000` → `巴特克的猛击`；`lang --range 10000 10012`
  正确列出聊天命令文本。
- **物品名链路**：`UnitAny.dwTxtFileNo` → ItemTxt（`wLocaleTxtNo` @ `+0xF4`）→ `LocaleText.get()`。
  `.vcb` 仅作**兜底**（`szCode -> 英文名`），主链路已全部走内存。
- ★**暗金 / 套装走另一张表**（2026-09-20 用户指出显示不对后补）：ItemTxt 那只给"底材名"
  （如 `赫拉迪克之杖的转轴`），游戏真正显示的是 UniqueItems.txt / SetItems.txt 的名字（`国王之杖`）。
  ```
  DataTables     = *(D2COMMON + 0x6FDE9E1C)        1.13c 绝对地址
  UniqueItemsTxt = *(DT+0xC24)  nUniqueItems = *(DT+0xC28)  rec=0x14C  wLocaleTxtNo @ +0x22
  SetItemsTxt    = *(DT+0xC18)  nSetItems    = *(DT+0xC1C)  rec=0x1B8  wLocaleTxtNo @ +0x24
  索引 = ItemData.dwFileIndex(+0x28)；用哪张表由 dwQuality 决定（5=套装 / 7=暗金）
  ```
  依据 hackmap `D2CallStub.cpp:56/111` + `d2structs.h` 的 `sgptDataTables` 注释。
  实测：DT=`0x6FDEFED8`、Unique n=538、Set n=127；暗金 fileIndex=124 → locale 2698 → `国王之杖`。
  实现 `items.QualityNameTables.locale(quality, file_index)`，hover 与物品清单共用。

**F. 单位命名（hover 显示名）** —— 统一入口 `d2h/acquire/names.py` → `UnitNamer.name()`

按 `UnitAny.dwUnitType` 分流，全部依据参考项目 `d2structs.h`（2026-09-20 实测通过）：

| 类型 | 链路 | 来源标签 |
|---|---|---|
| 0 玩家 | `pUnitData` → `PlayerData.szName[16]` @ `+0x00` | `PlayerData` |
| 1 怪物/NPC | `pUnitData` → `MonsterData.pMonsterTxt` @ `+0x00` → `MonsterTxt.wLocaleTxtNo` @ `+0x06` → 字符串表 | `MonsterTxt+内存字符串表` |
| 2 物件 | `pUnitData` → `ObjectData.pObjectTxt` @ `+0x00` → `ObjectTxt.wszName[64]` @ `+0x40`（宽字符，直接读） | `ObjectTxt.wszName` |
| 4 物品 | `UnitAny.dwTxtFileNo` → ItemTxt → 字符串表；**暗金/套装改走 Unique/Set 表**（见 E 段） | `ItemTxt+内存字符串表` / `暗金名(...)` |

- 物件名**不用绕 D2Lang** —— `ObjectTxt` 自带 `wszName`；`szName`(@`+0x00`, ascii 内部 token) 仅作兜底。
- 实测：储藏箱 → `你的私人储藏箱`；NPC → `恰西` / `基得`；物品 → `◇ 裂开红` / `短棍【普通】` / `ㄗ 瑟 Sur`；
  暗金 → `国王之杖`（底材名是 `赫拉迪克之杖的转轴`）。
- ⚠️ 表内存在名为「一股邪恶力量」的实体（游戏自带对无名/隐形单位的默认名），非解析错误，可忽略。

**G. 悬停开关 `D2WIN+0xCA664`（权威，2026-09-20 用户提供并实测）**

| 地址 | 含义 | 备注 |
|---|---|---|
| `D2WIN + 0xCA664` | **HoverFlag**：1=鼠标在可交互对象上（NPC/物件/物品，含 UI 内物品）；0=地面/空处 | 移开立即归零 |
| `D2WIN + 0xCA658` / `+0xCA65C` | 悬停框**屏幕坐标** (x, y)，实测 637~724 / 570~628 | ⚠️ **不是指针**，是整数 |
| `D2WIN + 0xCA66C` / `+0xCA670` | 疑似悬停框宽/高（109 / 99） | 待确认 |

- ★★**悬停文本内容 `D2WIN + 0xC9E58`（2026-09-20 反汇编 `DrawHoverText` 挖出，权威）**
  ```
  D2WIN+0x118F0 DrawHoverText(wStr, xPos, yPos, dwTran, dwColor)   ; hackmap d2ptrs.h:205
      wStr==NULL -> rep stosd 把 0x6F9A9E58 起 2KB 清零
      wStr!=NULL -> 拷进 0x6F9A9E58（wchar 缓冲，上限 0x400 字符）
      再把 x/y/dwTran/dwColor 写进框结构体 0x6F9AA658~0x6F9AA668
  D2WIN+0x133A0 DrawHover()  -> mov eax,0x6F9A9E58  取这段文本去画框
  ```
  ⇒ **游戏此刻显示的那行字，我们能原样读出来**（含 `ÿcX` 颜色符，`lang.strip_color()` 清洗）。
  实现 `game.read_hover_text()`，`hover` 输出里多一行 `悬停文本=...`。
  ★价值：这条链路**完全不依赖 UnitAny 指针** ⇒ 鼠标停在**地面物品名文本框**这类
  拿不到单位的场景（老大实测：旧指针对它无效）时，仍然知道指向的是什么。
  ⚠️ 框结构体在 `+0xCA658`，文本缓冲在 `+0xC9E58` —— 两个地址只差一位十六进制，**别搞混**。

- ✅**坐标含义已钉死（2026-09-20 用户实测）**：左右移动鼠标**只有 `+0xCA658` 变**，
  上下移动**只有 `+0xCA65C` 变**；对象挪到画面左上角时 x、y 都是个位数
  ⇒ 原点 = **客户区左上角**（不含窗口标题栏/边框，即 D2 的 800×600 画面坐标系）。
  NPC 也遵循同一规则（随 NPC 位置改变）。

- ★★**门控已默认关闭（2026-09-20 用户实机拍板，权威）**：
  ⚠️ **实测反例 —— 鼠标悬停在地面物品上时 `HoverFlag = 0`**，但 `HoverUnitId/HoverUnitType`
  反查出来的单位**是正确的**（地面那件 `融解药`，unitId=0x23）。
  ⇒ HoverFlag **不是「当前是否有悬停对象」的可靠判据**；拿它做门控会把真值屏蔽成空 ——
  历史 bug 正是这样：地面物品悬停时结构全空，反倒是不门控读出来的旧值才对。
  现在 `read_hover_unit(gate=False)`（默认）只把它当**读数**带出，不阻断解析；
  `hover --gate` 才恢复旧行为（仅供对照调试）。
- ~~UI 打开时残留上一个世界对象 → 命中 `UI_BLOCKS_HOVER` 禁止回退反查~~
  ★★**2026-09-20 已取消该阻断**：用户实机反馈「打开背包时鼠标指向 NPC 没响应、
  指向地面物品显示全为空」—— 那条规则把**正常的世界悬停一起误杀**了。
  现在面板名只作为提示放进 `read_hover_unit()["ui"]` 并打印，**绝不阻断解析**。
  ⚠️ 教训（两次同源）：**任何"我觉得这个值不可信所以屏蔽掉"的规则都会误伤真值** ——
  宁可多打一个可能陈旧的值让人自己判断，也不要替用户决定"这不是"。
- ★★**沿触发（2026-09-20 老大方案，取代门控）** —— **两个标记一起判，谁跳变就从谁那条路取**：
  | 触发键 | 标记地址 | 管的路径 | 解析顺序 |
  |---|---|---|---|
  | `ground` | `D2CLIENT+0x11C2F8` | **地面物品** / 地面物品名文本框 | 直走 `hover_id` 反查（**跳过 CurrentViewItem**，免得把背包里的物品当成地面对象） |
  | `unit` | `D2WIN+0xCA664` | **NPC / 物件 / UI 内物品** | `CurrentViewItem` 优先 → `hover_id` 反查 |
  | `npc` | `D2CLIENT+0x11C2F4` | 对 NPC 也有响应 | **仅观测打印**，不单独触发取样 |
  | `ptr` | `CurrentViewItem` / `hover_id` / `hover_type` | **补充触发**：两个标记都没跳、但指针或单位标识变了 | 全链取一次 |

  规则：① 标记值变化 ⇒ 从对应路径取一次；② **都没跳变 ⇒ 不刷新**（这才是「移开后一直
  显示上一个对象」的正解 —— 旧实现每帧都取，陈旧值就被当成当前对象）；
  ③ 非 0 -> 0 的**下降沿**判为「已移开」，单位值照打但标注仅供参考；
  ④ **`ptr` 兜底（2026-09-20 老大补）**：两个标记都没跳但 `CurrentViewItem` / `hover_id` /
  `hover_type` 变了 ⇒ 也算真实变动，全链取一次。典型场景：鼠标从一个物品滑到另一个，
  标记恒为 1 不跳，只有指针在变 —— 没有这一路就会漏掉整个「同类型内换对象」的操作。
  实现：`game.read_hover_ptrs()` 只读这三个值（不反查 unit 表，开销极小）。
  首帧走全链取一次作基线；`--no-trigger` 退回每帧两条路都取；`--raw` 每帧都打。
  实现：`game.HOVER_TRIGGER_KEYS` / `game.read_hover_marks()` / `read_hover_unit(trigger=...)`。
  ★ 两个标记**互补**：地面物品那档 HoverFlag 不抬（实测=0），正由 `0x11C2F8` 补上。

- ✅**来源优先级（自上而下取第一个能用的，2026-09-20 定，与 flag 无关）**：
  | # | 来源 | 用途 |
  |---|---|---|
  | 1 | `CurrentViewItem(0x11BC38)` | UI（背包/仓库/商店）内物品、地上物品 —— 鼠标在仓库宝石间移动时精确跟随 |
  | 2 | `SelectedUnitFlag(0x11C2F4)` / `Flag2(0x11C2F8)` | ⚠️ 实测是标记，校验基本跳过；仅作诊断打印 |
  | 3 | `(hover_id 0x6FBC9638, hover_type 0x6FBC964C)` | 反查 unit 表 —— 悬停卡夏/瓦瑞夫/储藏箱靠它 |
  | 4 | `D2WIN+0xC9E58` 悬停文本 | **拿不到单位时的保底**（地面物品名文本框） |
  每一级都过 `_try_ptr()` 校验，失败自动退到下一级。
- ✅**去抖已完全移除（2026-09-20，用户反馈"换物品反应慢"）**：不再等第二帧确认，
  任一字段变动当帧即取一次完整结构并打印。采样间隔默认 `0.15s`（原 0.5s，`--interval` 可调）。
  旧参数 `--no-debounce` 已删除。
  ⚠️ 教训：权威开关本身已经是"游戏内部状态"，再用采样次数去抖等于人为加延迟，只会拖慢。
  输出里的 `Sel=/Sel2=/ViewItem=` 三个原始值常驻打印，便于肉眼交叉验证来源。
- `--gate`：开启 HoverFlag 门控（**默认关闭**，见上）。`--raw`：每帧都打完整结构。

**H. 地面物品枚举（房间邻近表，2026-09-20 落地 `game.enumerate_ground_items()`）**

- ✅ 入口 `cli.py ground`（菜单 21）。**不依赖鼠标悬停**，主动列出附近房间里躺在地上的物品。
  ```
  pPlayer     = *(D2CLIENT PlayerUnit)
  pPath       = *(pPlayer + 0x2C)
  pRoom1      = *(pPath   + 0x1C)      ★入口
  paRoomsNear = *(pRoom1  + 0x00)      ★二级指针：房间 i = u32(pa + i*4)
  dwRoomsNear = *(pRoom1  + 0x24)
  pUnitFirst  = *(room    + 0x74)      链表 *(p + 0xE8)
  判定 dwUnitType==4 && u8(pUnitData + 0x69)==0
  ```
- ⚠️ 两个坑（都实测踩过，改代码前先看）：
  ① 入口是 **`pPath + 0x1C`** 不是 `pPlayer + 0x1C` —— 后者是 `pDrlgAct`，其 `+0x24` 恒 0
     ⇒ **静默返回「房间 0 个」且不报错**，极难发现；
  ② **`paRoomsNear` 是二级指针**，直接 `u32(pRoom1 + i*4)` 会读到指针本身
     （`i=4` 恰命中 `pRoom2` ⇒ 「链长 1、type 0」的假房间）。
  ③ `hackmap d2structs.h` 把 `dwRoomsNear` 注成 `//+04` 是**笔误**（与 `_1[3]` 重号），实际 **+0x24**；
     `pUnitFirst = +0x28 + 19*4 = +0x74`（头文件未标）。
- 坐标统一走 `read_unit_pos()`（物品是 StaticPath）；名称由 `UnitNamer` 解析。
  跨房间/跨链表**按指针去重**（`seen_units`），同一单元只计一次。

**I. 物品词缀表 / 属性表（2026-09-20 落地 `acquire/itemprops.py`，入口 `cli.py props`）**

- **取行公式（★老大实机反汇编，权威，优先于任何推断）**：
  ```
  稀有取行  D2Common+0x71790 : lea eax,[ecx+eax*8-0x48]  ->  rec = 表址 + (值-1)*0x48
  词缀取行  D2Common+0x71841 : lea eax,[eax+ecx-0x90]    ->  rec = 表址 + (值-1)*0x90
  ```
  ⇒ 两条都是 **1 基（值-1）**，值=1 命中表首行，id=0 = 无。

- ★★**该公式已用实机 roll 值证实（2026-09-20）—— 不用鼠标、不用悬停**，方法可复用：
  拿一件稀有物品（`rP=162 rS=126` 的稀有戒指，6 条属性全是实机 roll 出来的定值），把每个词缀 id
  **两个候选行的 `nMin/nMax`** 与该物品的实际属性值对表，**谁容得下就是谁**：

  | 词缀 id | (值-1) 采信 | (值) 另一解 | 物品实际值 | 结论 |
  |---|---|---|---|---|
  | 后缀 174 | 行173 `of the Apprentice` **10-10** | 行174 `of the Magus` 20-20 | FCR = 10 | **采信** |
  | 后缀 286 | 行285 `of Chance` **5-15** | 行286 `of Fortune` 16-25 | MF = 10 | **采信** |
  | 前缀 1156 | 行1155 `Beryl` **5-10** | 行1156 `Viridian` 11-20 | PR = 10 | **采信** |
  | 前缀 987 | 行986 `Steel` **41-60** | 行987 `Silver` 61-80 | AR = 43 | **采信** |

  ⇒ 4 条独立属性一致指向 **(值-1)**，**已定死**（`wRarePrefix/wRareSuffix` 同理）。
  代码仍两解都打（规范 §12 不裁字段），但「采信」列现在是**已证实**，不再是「待实机确认」。
  `props --hover` 的悬停文本对答案**保留**，用作以后换物品/换版本的回归手段。

- **gpt 描述区**（`gpt = D2COMMON + (0x6FDEFB94 − DLLBASE)`，**是基址本身，不解引用**）关键槽位：

  | 槽 | 内容 | 实测 |
  |---|---|---|
  | `+0x00` | nItemsTxt | 716 |
  | `+0x2C` | **0x90 块基址 B** | rec0 `of Health` |
  | `+0x30` | B + 747 行 | rec748 `Sturdy`（rec747 是空行） |
  | `+0x34` | B + 1416 行（71 行） | rec1416 `Fletcher's` |
  | `+0x38` | 第三段行数 | 0x47 = 71 |
  | `+0x50` | **0x48 块基址 RS** | 名字串在 `+0x26`、locale 在 `+0x0C` |
  | `+0x54` | RS + 155 行 | `Beast`…`Corruption`（46 行） |
  | `+0x48` | 计数 | 201 = 155 + 46（**实测吻合**） |

- **0x90 块分段（逐行打名字核出来的，不是推算）**：值 `1..747` 后缀区 →
  值 `748..1416` 前缀区 → 值 `1417..1487` 第三段（71 行）。
  实机 128 件物品统计：`wMagicSuffix` ∈ [115,742]、`wMagicPrefix` ∈ [892,1334]、
  `wAutoPrefix` ∈ {1425,1441,1443} —— **全部落在各自区段内，无一例外**。
  ⚠️ 第三段身份**推断 = automagic 自动前缀**（①与 magic 块同块无缝：`T3 == SUF + 1416*0x90` 实测 True；
  ②wAutoPrefix 取值落在该段；③行名是低阶/职业词条），**未经老大确认前不当硬事实**。

- **0x48 块 = 稀有物品「名字词」表**：值 `1..155` 后缀词（`bite`…`flange`）、
  值 `156..201` 前缀词（`Beast`…`Corruption`）。
  实机 `wRarePrefix` ∈ [159,189]、`wRareSuffix` ∈ [64,135] ⇒ 解释了「wRarePrefix 为何 > 46」。
  ⇒ `wRarePrefix/wRareSuffix` 是**显示名用词**索引（名字 = 前缀词 + 后缀词），
  物品的 mod 早已写进 StatList，与这两个字段无关。稀有显示名由 `itemprops.rare_name()` 拼。

- **词缀记录 `0x90` 字段**：`+0x00 szName[32]`、`+0x20 WORD wLocaleTxtNo`、
  `+0x24 nGroup`（同组=同词缀不同档：Sturdy/Strong 同组 5）、`+0x2C nMin`、`+0x30 nMax`。
  依据：第三段 71 行的这三列与「生命 13 组 1-5/6-10/…/41-60、法力 11 组、全抗 41 组」逐行吻合。

- **符文之语物品**（quality=2 + RUNEWORD 标志）：`wMagicPrefix[0]` 存的是**符文之语名的 locale id**，
  **不是词缀索引**（实测 20525→`白天 Daylight`、20635→`精神 Spirit`、20651→`时间 Time`）。

- **属性显示值 = 原始值直读（系数 1）**，用悬停文本对过答案（runeword「时间」Demonhead）：
  防御 175、MDR 7、四抗 15、毒抗上限 5、经验 3、IAS 20、FBR 18、FCR 30 —— 逐条相等。
  ⚠️ `ItemStatCost` 的 `+0x0C/+0x10`（hackmap 标 dwDivide/dwMultiply）**在本机 1.13c 不成立**
  （`+0x0C` 恒 1024 ⇒ 折算会得到「防御 175 → 1.709」这种荒谬值）。**禁止再用它们折算**；
  hp/mana 家族（stat 6/7/8/9/10/11）只额外给 `raw>>8` 作**候选**并标注未验证。
  ★ 2026-09-20 新增证据（**仍标"候选"**）：Spirit 统治者大盾 `stat=9(Mana) raw=24576` ⇒ `÷256 = 96`，
  正落在 Spirit 的 89~112 随机区间内 ✓（1 例；待第二条样本再升为"已验证"）。

- ★★**物品链表：兄弟链必须走 `ItemData+0x64 (pNextInvItem)`**（2026-09-20 实测，**静默漏数**坑）：
  `UnitAny+0xE8 (pListNext)` 对**背包内物品与镶入物恒为 0** ⇒ 循环第一件就 break，**不报错**。
  同一玩家 UnitInventory（`pFirstItem=0x03D9F200`）实测对照：
  · 沿 `+0xE8` → **1 件**
  · 沿 `+0x64` → **89 件**（背包 + 腰带 + 身上，实机清单合理：护身符/符文/宝石/暗金/稀有/方块/书）
  ⚠️ **别再把这个坑当"某次统计跑在残缺数据上"的理由**（2026-09-20 老大纠正）：
  物品件数在**不同角色之间本来就不同**，`props` 早期只出 5 件是因为**当时用的是另一个号**。
  本坑的证据只来自**同一角色、同一次快照**下的两条链对照（1 ↔ 89），与历史件数无关。
  落地：`game.enumerate_inventory()` 与 `itemprops.socket_children()` 都是 **+0x64 优先、+0xE8 兜底**。
  验证：符文之语 Spirit 统治者大盾 4 孔 ⇒ 4 个符文 `Tal/Thul/Ort/Amn`（**= 官方配方**），
  且四条属性与「符文镶盾」效果逐条吻合（Tal +35PR / Thul +35CR / Ort +35LR / Amn 攻击者受伤 14）。

- ★**完整显示名拼接**（`itemprops.ItemProps.display_name()`，2026-09-20；`hover` 指向物品时自动打印）：
  · 归类判定顺序：符文之语(`flags&RUNEWORD`) > 暗金(7)/套装(5) > 稀有(6) > 魔法(4)/手工(8) > 其它。
  · ★★**顺序是「前缀 后缀 底材」**，**不是**英文版的「前缀 底材 后缀」——
    **实机对过答案**：悬停文本框给的是 `燃烧之 工匠的 ☆超大型护身符`（魔法护身符 mP=1189/mS=676）。
    连接字（"之"/"的"）**已含在词条本身里**，不要自己加。
    代码同时打三行：`完整名(主推)` / `完整名(备选=英文顺序)` / `完整名(无空格)`。
  · 部件来源全部是内存：底材 = `ItemTxt+0xF4` → 字符串表；词缀名 = 0x90 块；稀有名字词 = 0x48 块；
    符文之语名 = `ItemData+0x38[0]`（那是 **locale id**）。每个部件在输出里都带行号/区段/locale 出处。
  · ⚠️ 稀有物品的 `magic_prefix/suffix` 是**隐藏 mod 词缀**（只影响属性、不进名字）；
    非魔法/手工品质下该数组同样不入名（原始值照打，不裁剪）。
  · ⚠️ 未验证项：暗金/套装（主推纯表名）/ 符文之语（主推「符文名 底材」）**这两类没对过答案**；
    上等/低质的质量词（Superior/Crude 之类）**没有拼**进名字。

- ★**菜单 15 / 16（`hover` / `hover --watch`）指着物品时的三段输出**（2026-09-20 老大要求「把词条和属性的显示也接上」）：
  · ① 完整显示名（每个部件带出处）② **词条全组**（槽/id/解析方式/采信行+区段+name+internal+locale/另一解）
    ③ **属性 StatList 全组**（list/stat/param/raw/show/desc/text/ItemStatCost 原样）；
    另列孔内物品「指针 + 代码名 + 属性条数」（逐条属性仍走 `props --index N`）。
  · ★★实现要点：词条/属性段与 `props` 报告**共用同一函数**（`cli._print_rep_affixes` / `cli._print_rep_stats`）
    —— 两处永远同源同字段。**别再各写一份**：历史上同一段逻辑抄两遍，改一处漏一处。
  · 入口 = `cli._print_item_hover_details()`（在 `_dump_hover` 里 `type==4` 触发）；
    **任何一步失败都降级为打印原因，绝不抛异常** —— watch 每帧都会走到这里，崩一次监听就断了。
  · 观察（**未归因，按 §3.7 只记录**）：魔法护身符 4 条 StatList 里有 2 条（stat=160 / stat=188 param=8）
    与「燃烧之 / 工匠的」看不出对应关系，原样列出、不解释是谁给的，留给实机校验。

- ⚠️ **词缀表只在游戏内可读**：离开游戏后整块被清零 ⇒ `tables_readable()` 为假时显式告警，
  **绝不静默返回空**（曾因此被误判成偏移写错）。

---

## 16. 临时文件约定（2026-09-19 定死）

- **唯一临时目录 = `<项目根>/temp/`。** 一切临时文件、探针脚本、中间输出、一次性扫描结果，**一律落在项目内 `temp/`**，**禁止**写入 C 盘 `%TEMP%` / `%TMP%`、系统目录或用户目录。
- **理由**：留在项目内才能被 `.gitignore` 统一收纳、随项目一起清理、换机器不散落；写系统盘既污染环境又找不回来。
- **代码级落地**：`d2h/paths.py` 是路径的唯一权威，提供 `TEMP` / `tmp_path()` / `write_tmp()` / `clean_temp()` / `temp_info()`。
  - `tmp_path()` 拒绝绝对路径与含 `..` 的名字（抛 `ValueError`），从物理上堵死写系统目录的可能。
  - 其他模块不得自行拼 `Path(...) / "temp"` 或调用 `tempfile.gettempdir()`，统一 `from d2h import paths`。
- **gitignore**：`temp/*` 全部忽略，仅保留 `!temp/.gitkeep` 占位（保证目录存在且可入库）。
- **清理**：`python d2h/cli.py tmp` 查看概况，`tmp --clean` 一键清空（保留 `.gitkeep`）。开发过程中产生的一次性脚本用完即清。

### 16.1 运行日志：控制台与文件**同步双写**（2026-09-20 定死）

- **一条规则**：**屏幕上出现的 = 盘里有的**。`print()` / `logging` / traceback **全部**落盘，
  不许只写其中一种。
  - 背景：此前只有 `LOGGER` 挂了 FileHandler ⇒ 满屏 `print()` 一条都没进文件，`logs/` 里
    每个文件才 186~255 字节（只有启动/结束两行），排障时等于没有日志。
- **落地**：`d2h/runlog.py` = `Sink`（带额度控制的落盘器）+ `Tee`（同时写文件与控制台）；
  `cli.setup_logging()` 负责安装。**所有入口都必经 `cli.main()`**（bat 与 `tools/capture.py`
  都一样），所以只在这一处安装即可覆盖全部打印。
- **为什么不去 bat 里挂管道**：① 批处理没有原生 tee，`cmd` 管道写法会吃掉交互式菜单的 stdin；
  ② `watch` / `hover --watch` 靠 Ctrl+C 结束，管道的收尾与缓冲行为不可靠。
- ⚠️ **安装顺序**（改 `setup_logging()` 时必须保持）：
  ① 先给 LOGGER 挂 `StreamHandler(sys.stdout)` —— 此刻捕获的是**原始** stdout；
  ② 再 `runlog.install(sink)` 替换 `sys.stdout` / `sys.stderr`。
  **反了的后果**：LOGGER → Tee → 文件，同一行在文件里**出现两遍**。
- ⚠️ **logging 的 handler 必须用 `Sink` 而不是原始文件句柄**：`watch` 类命令的输出
  **全走 LOGGER**，直写句柄会**绕过大小上限**（上限形同虚设）。实测：3000 行 LOGGER 在
  20 KiB 上限下正确截停在 21465 字节（而非 210 KiB）。
- ⚠️ **先写文件、再写控制台**：控制台若因 GBK 抛 `UnicodeEncodeError`，盘里仍留有完整内容
  （原行为是屏幕炸 + 什么也没留下）。
- **命名 / 开关**（`src` 由 bat 内设置的环境变量指定）：

  | 项 | 值 |
  |---|---|
  | 文件名 | `logs/d2h_<YYYYmmdd_HHMMSS>_<src>.txt` |
  | `src` | `D2H_LOG_SRC`：`run`(run.bat) / `watch`(watch.bat) / `uiwatch`(uiwatch.bat) / `agent`(tools/capture.py) / 默认 `cli` |
  | 指针文件 | `logs/latest.txt` 永远指向最近一次日志（bat 结尾只提示这一个路径） |
  | 关闭 | `D2H_LOG=0` ⇒ `setup_logging()` 返回 `None`，**不创建文件** |
  | 大小上限 | `D2H_LOG_MAX_MB`，默认 8 MiB；超限**停写文件 + 告警一次**，控制台继续输出 |

- **日志设施自身故障**（写盘失败）只许告警一次并停写，**绝不上抛**、也不得掩盖原异常
  （沿用 §17 铁律①）；告警走 `sys.__stdout__`，不混进被抓取的正文。
- **文件头**由 `runlog.write_header()` 写（时间 / `src` / argv / cwd / python / 上限），只进文件、不进控制台。
- **不进 git**：`logs/` 与 `temp/` 一样已在 `.gitignore`。

---

## 17. 报错现场快照（自动抓取，2026-09-20）

- **触发时机**：任何子命令抛异常时，**入口自动**抓一份现场（`main` 顶层 `except` 与菜单循环 `except` 两处都接了 `_auto_errsnap`）。正常路径零开销。
- **存哪里**：`temp/err_<时间戳>_<命令>/`（沿用 §16 的临时目录约定，随 `err --clean` 清理）。
- **内含三件**：
  1. `error.txt` —— 命令行、目标类型、异常类型与消息、完整 traceback；
  2. `context.json` —— PID、各模块真实基址、Fog 状态码、UI 面板开合、玩家单位、悬停单位读数；
  3. `D2CLIENT.bin` —— D2CLIENT 模块完整转储（~1.2MB），可**离线按偏移重读**，回答"报错那一刻这里到底是什么值"。
- **用途**：报错信息与现场**绑定**，事后不必复现即可对照校验；定位或修完之后 `cli.py err --clean` 删除，不留垃圾。
- **三条铁律**：
  ① 抓快照自身失败**绝不能**掩盖/替代原始异常（内部全部 `_safe` 包裹，失败只写 debug 日志）；
  ② 全程只读（`PROCESS_VM_READ`），不写游戏内存；
  ③ 不进 `snapshots/` —— 那是人工标记的正常快照，语义不同。
- **命令**：`cli.py err`（列出）/ `err --show <名字>`（明细）/ `err --clean`（清空）；菜单 18 / 19。

---

## 18. 风险与开放问题

1. **（已确认）** 约束语义：指"对游戏进程内存的访问策略"（读 / 不写 / 可拍快照）。
2. **（已锁定）** 版本：**仅 1.13c**（去掉 1.13d）。
3. **（已决定，延后）** 展示层技术栈（PyQt / 游戏内 overlay / 独立窗口）仍开放，但已确定**最后做（M6）**。
4. **开放**：快照存储介质默认"目录 + 文件"，后续可换 SQLite，M2 细化；`snap --full` 全量提交区域转储待实现。
5. **风险**：参考项目部分结构体依赖运行时指针，纯静态移植需实测校准；以快照为测试基准降低风险。

---
*本规范为起点，随 M0–M6 推进持续修订。修订时优先回查参考项目，保持"最优先参考"原则。*
