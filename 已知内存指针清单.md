# D2H 已知内存指针 / 监听清单（Diablo II 1.13c）

> 版本锁 **1.13c**；目标进程实际为 **D2loader.exe**（无独立 game.exe）。
> **只读**：本清单里所有地址仅用于 `ReadProcessMemory`（仅申请 `PROCESS_VM_READ`）。
> 任何"写内存 / Patch / 远程调用"都超出本项目范围（规范 §2.1）。
>
> 来源：参考项目 `d2hackmap`（`d2ptrs.h` / `d2vars.h` / `d2structs.h`）+ 本机实机实测与只读反汇编。
> 代码权威（本文件与代码不一致时以代码为准）：
> `d2h/acquire/offsets.py`、`game.py`、`items.py`、`itemprops.py`、`lang.py`、`structs.py`；
> 观测点全表另见 `规范.md §15.1`；
> **数据表（.tbl 字符串表 / .txt 数据表）指针另见《`已知数据表指针清单.md`》**（本册不重复）。

## 0. 读法约定（先看这段，否则地址没法用）

| 记号 | 含义 |
|---|---|
| `DLL+偏移` | **推荐用法**。相对该 DLL 的偏移，抗 ASLR / 重定位 |
| `0x6Fxxxxxx` | 参考项目口径的「**1.13c 默认基址下的绝对地址**」，本文件仅作对照 |
| 换算 | `真实地址 = 真实模块基址 + (绝对地址 − DLLBASE[该DLL])` |
| 代码 | `offsets.resolve(bases, "D2CLIENT", "HoverUnitId")` / `read_ptr()` / `read_byte()` |
| `bases` | `offsets.collect_module_bases(handle)`，WOW64 必须用 `EnumProcessModulesEx(LIST_MODULES_32BIT)` |
| 状态 | `[V]` 按键/实机实测通过；`[U]` 用户实机核验；`[?]` 只有 hackmap 命名、未辨明；`[存档]` 已搬进代码但本工具暂未使用 |

⚠️ **绝不能用默认基址硬套**：D2Game / D2Lang 等会被重定位，必须取进程内的**真实模块基址**。

### 0.1 DLL 默认基址（换算用）

| 逻辑名 | 文件 | DLLBASE（1.13c） |
|---|---|---|
| D2CLIENT | D2Client.dll | `0x6FAB0000` |
| D2COMMON | D2Common.dll | `0x6FD50000` |
| D2WIN | D2Win.dll | `0x6F8E0000` |
| D2LANG | D2Lang.dll | `0x6FC00000` |
| D2GFX | D2gfx.dll | `0x6FA80000` |
| FOG | Fog.dll | `0x6FF50000` |
| D2GAME | D2Game.dll | `0x6FC20000` |
| D2MULTI | D2Multi.dll | `0x6F9D0000` |
| D2NET | D2Net.dll | `0x6FBF0000` |
| BNCLIENT | BNClient.dll | `0x6FF20000` |
| STORM | Storm.dll | `0x6FFB0000` |
| D2CMP | D2CMP.dll | `0x6FE10000` |
| D2LAUNCH | d2launch.dll | `0x6FA40000` |
| D2MCPCLIENT | D2MCPClient.dll | `0x6FA20000` |

### 0.2 命令行直接读一个指针链（不用写代码）

```
python d2h/cli.py probe "FOG+0x4AFE0,+0x8"          # 状态码
python d2h/cli.py probe "D2CLIENT+0x50D00,+0x10"   # UI 面板数组某格
python d2h/cli.py probe "0x6FBC9638"               # 也接受绝对地址（仅调试）
```

---

## 1. 判态：是否在游戏（状态码）

| 项 | DLL+偏移 | 绝对地址 | 读法 | 状态 |
|---|---|---|---|---|
| 状态码链 | `FOG+0x4AFE0` | `0x6FF9AFE0` | `status = *( *(Fog+0x4AFE0) + 0x08 )` | [V] |

语义（用户确认）：

| 值 | 含义 |
|---|---|
| `11` | 战网登录界面（不在游戏） |
| `14` | 人物选择界面（不在游戏） |
| `2000..2999` | 战网(BN) 游戏内 |
| `3000..3999` | 单机游戏内 |
| `4096` | 确在游戏内 |
| `>999` 其它 | 未归类，按"游戏内"处理 |
| `<1000` | 登录/选人界面，不在游戏 |

| 项 | DLL+偏移 | 绝对地址 | 读法 | 状态 |
|---|---|---|---|---|
| InGame 兜底字节 | `D2CLIENT+0x11C3A0` | `0x6FBCC3A0` | `u8(addr)==1` ⇒ 在游戏 | [V]（仅兜底） |

落地：`game.read_game_state()` / `classify_state()` / `in_game_status()`。

---

## 2. ★ 悬停监听（本项目监听逻辑的核心）

### 2.1 触发标记（三路 + 一路兜底）

监听**不是**定时刷新，而是这 6 个字段**任一变化**才取一次完整结构。

| 触发键 | DLL+偏移 | 绝对地址 | 响应对象（实测判读） | 状态 |
|---|---|---|---|---|
| `ground` | `D2CLIENT+0x11C2F8` | `0x6FBCC2F8` | **地面物品**（含地面物品名文本框） | [V] |
| `unit` | `D2WIN+0xCA664` | `0x6F9AA664` | **NPC / 物件 / UI 内物品** | [V] |
| `npc` | `D2CLIENT+0x11C2F4` | `0x6FBCC2F4` | NPC（**仅观测，不单独触发取样**） | [V] |
| `ptr`（非地址） | — | — | 两个标记都没跳但下面 §2.2 的指针/标识变了 ⇒ 也算真实变动 | [V] |

- `ground` + `unit` **互补**：地面物品那档 `HoverFlag` 不抬（实测 = 0），正由 `ground` 补上；
  移开时两者都回落 ⇒ 不会再拿陈旧值当"当前对象"。
- 下降沿（非 0 → 0）= 该路径的鼠标已移开。
- `ptr` 那一路专门抓"**同类型内换对象**"：从一个物品滑到另一个，标记恒 1 不跳，只有指针在变。
- 落地：`game.HOVER_TRIGGER_KEYS` / `HOVER_TRIGGER_DESC` / `read_hover_marks()` / `read_hover_ptrs()`。

### 2.2 单位指针与单位标识

| 项 | DLL+偏移 | 绝对地址 | 含义 | 状态 |
|---|---|---|---|---|
| CurrentViewItem | `D2CLIENT+0x11BC38` | `0x6FBCBC38` | `UnitAny*`，**UI 内物品**（背包/仓库里的那件） | [V] |
| HoverUnitId | `D2CLIENT+0x119638` | `0x6FBC9638` | 悬停单位的 `unitId`（不是指针，是 DWORD） | [V] |
| HoverUnitType | `D2CLIENT+0x11964C` | `0x6FBC964C` | 悬停单位类型（0玩家/1怪/2物件/3导弹/4物品/5地块） | [V] |

来源（只读反汇编 `GetSelectedUnit = D2CLIENT+0x51A80`）：

```
mov edx,[0x6FBC964C] ; shl edx,9 ; add edx,0x6FBBA608   ← 块索引 = 类型
mov ecx,[0x6FBC9638] ; and eax,0x7F                       ← 桶索引 = unitId & 0x7F
```

**来源优先级**（与任何 flag 无关）：`CurrentViewItem`（UI 内物品）→ 两个标记对应的路径（`ground` 跳过 CurrentViewItem，`unit` 用它）→ `(hover_id, type)` 反查 unit 表 → **悬停文本保底**。

### 2.3 悬停框 / 悬停文本（D2WIN）

结构体由只读 dump `D2WIN.DrawHoverText(D2WIN+0x118F0)` 钉死：

```
6F8F1933  mov [0x6F9AA658], ebx   ; xPos
6F8F1939  mov [0x6F9AA65C], eax   ; yPos
6F8F193E  mov [0x6F9AA660], ecx   ; dwTran（透明度）
6F8F1944  mov [0x6F9AA664], edx   ; dwColor（颜色/有效位）= 本项目所称 HoverFlag
6F8F194A  mov [0x6F9AA668], 0
```

| 项 | DLL+偏移 | 绝对地址 | 含义 | 状态 |
|---|---|---|---|---|
| HoverX | `D2WIN+0xCA658` | `0x6F9AA658` | 悬停框屏幕坐标 x（**坐标不是指针**，原点=客户区左上角，800×600 系） | [V] |
| HoverY | `D2WIN+0xCA65C` | `0x6F9AA65C` | 悬停框屏幕坐标 y | [V] |
| HoverTran | `D2WIN+0xCA660` | `0x6F9AA660` | 透明度 | [存档] |
| HoverFlag | `D2WIN+0xCA664` | `0x6F9AA664` | 1=鼠标悬停在可交互对象上；⚠️ **地面物品悬停时也可为 0** ⇒ 不作判据 | [V] |
| HoverRectL/T/R/B | `D2WIN+0xCA66C` / `+0xCA670` / `+0xCA674` / `+0xCA678` | `0x6F9AA66C` / `670` / `674` / `678` | 框矩形（实测 109/99，疑似 right/bottom，**待确认**） | [?] |
| **HoverTextBuf** | `D2WIN+0xC9E58` | `0x6F9A9E58` | **悬停文本本体**：内联 UTF-16 缓冲，最长 0x400 字符（2KB），`rep stosd` 清零 | [V] |

★ `HoverTextBuf` 是「地面物品名」的唯一解，且**不依赖任何指针**：游戏此刻画在框里的那行字我们直接读出来（含颜色符 `ÿcX`）。

### 2.4 反查单位：unit hash 表

| 项 | 值 | 说明 |
|---|---|---|
| 表基址 | `D2CLIENT+0x10A608`（`0x6FBBA608`） | `VARS.D2CLIENT.UnitTable` |
| 分块 | 每块 **512 字节** = 128 桶 × 4 字节 | **块索引 = 单位类型**（0..5），`块 = 基址 + 类型*512` |
| 桶 | `unitId & 0x7F` | 桶内是 `UnitAny*` 链表 |
| 链表 | `UnitAny.pListNext` @ `+0xE8` | 沿它走同桶下一个单位 |

落地：`game.find_unit_by_id()`。
⚠️ `+0xE8` 对**背包内物品 / 镶入物恒为 0**：物品链要走 `ItemData+0x64 (pNextInvItem)`。

### 2.5 ⚠️ 三个「看着像指针、其实不是」的坑（都踩过）

| 地址 | 真实身份 | 误判后果 |
|---|---|---|
| `D2CLIENT+0x11C2F4` / `+0x11C2F8` | **标记位**（0/1），不是 `UnitAny*` | 当指针解引用 ⇒ 崩溃 / 报"陈旧指针"；`GetSelectedUnit` 里的 `test eax,eax` 是条件判断，不是取返回值 |
| `D2WIN+0xCA658` / `+0xCA65C` | 悬停框**屏幕坐标** | 当指针解引用 ⇒ 读到垃圾 |
| `ItemData+0x5C (pOwnerInventory)` | **未打孔物品**指向持有者（玩家）背包 | 当"孔内子物品链"用 ⇒ 拿到无关物品；必须按 `SOCKETED` 标志门控 |
| `UnitAny+0x60` vs `ItemData+0x5C` | 自己的 inventory vs 持有者 inventory | 用错 ⇒ 孔内物品数为 0 或混入背包物品 |

---

## 3. 游戏内 UI 面板标志数组（38 项）

读法：`p = *(D2CLIENT+0x50D00)`；**数组起点 = p − 4**；每项 DWORD，`偏移 = index*4`。
⚠️ 坑：曾把 `p` 当数组起点 ⇒ **全部偏移少 4、整体错位一格**。

```
p      = u32(D2CLIENT + 0x50D00)      # 实测 p = 0x6FBAAD84
数组    = p - 4                        # 0x6FBAAD80
item i = u32(数组 + i*4)
```

命名权威 = hackmap `d2vars.h` 的 `enum UIVar`。

| 偏移 | idx | 名称 | 侧 | hackmap | 热键 | 状态 |
|---|---|---|---|---|---|---|
| `+0x00` | 0 | 数组头(恒 1) | - | UNK0 | - | [V] |
| `+0x04` | 1 | 背包 | 右 | INVENTORY | I | [V] |
| `+0x08` | 2 | 属性 | 左 | STATS | C | [V] |
| `+0x0C` | 3 | 技能组(左右手) | - | CURRSKILL | - | [U] |
| `+0x10` | 4 | 技能树 | 右 | SKILLS | T | [V] |
| `+0x14` | 5 | 聊天输入 | - | CHATINPUT | ENTER | [U] |
| `+0x18` | 6 | 新属性点按钮 | - | NEWSTATS | - | [U] |
| `+0x1C` | 7 | 新技能点按钮 | - | NEWSKILL | - | [U] |
| `+0x20` | 8 | NPC 对话框 | - | INTERACT | - | [U] |
| `+0x24` | 9 | 设置 | - | GAMEMENU | ESC(无 UI 时) | [V] |
| `+0x28` | 10 | 地图 | - | AUTOMAP | TAB | [U] |
| `+0x2C` | 11 | 配置快捷键 | - | CFGCTRLS | - | [U] |
| `+0x30` | 12 | 商店(NPC 交易) | - | NPCTRADE | - | [U] |
| `+0x34` | 13 | 显地面物品 | - | SHOWITEMS | ALT | [U] |
| `+0x38` | 14 | 打孔/注入窗 | - | MODITEM | - | [U] |
| `+0x3C` | 15 | 任务 | 左 | QUEST | Q | [V] |
| `+0x40` | 16 | UNK16 | - | UNK16 | - | [?] |
| `+0x44` | 17 | 任务日志按钮 | - | NEWQUEST | - | [U] |
| `+0x48` | 18 | 下方面板 | - | STATUSAREA | - | [?] |
| `+0x4C` | 19 | UNK19(初值 1) | - | UNK19 | - | [V] |
| `+0x50` | 20 | 传送 | - | WAYPOINT | - | [U] |
| `+0x54` | 21 | 迷你标签栏 | - | MINIPANEL | - | [U] |
| `+0x58` | 22 | 组队 | - | PARTY | P | [V] |
| `+0x5C` | 23 | 玩家交易 | - | PPLTRADE | - | [U] |
| `+0x60` | 24 | 信息页(消息日志) | - | MSGLOG | - | [U] |
| `+0x64` | 25 | 仓库 | 左 | STASH | - | [V] |
| `+0x68` | 26 | 盒子 | 左 | CUBE | - | [U] |
| `+0x6C` | 27 | UNK27 | - | UNK27 | - | [?] |
| `+0x70` | 28 | 背包 2 | - | INVENTORY2 | - | [?] |
| `+0x74` | 29 | 背包 3 | - | INVENTORY3 | - | [?] |
| `+0x78` | 30 | 背包 4 | - | INVENTORY4 | - | [?] |
| `+0x7C` | 31 | 腰带 | - | BELT | - | [?] |
| `+0x80` | 32 | UNK32 | - | UNK32 | - | [?] |
| `+0x84` | 33 | 帮助 | - | HELP | H | [U] |
| `+0x88` | 34 | UNK34 | - | UNK34 | - | [?] |
| `+0x8C` | 35 | 玩家头像列表 | - | PARTYHEAD | - | [U] |
| `+0x90` | 36 | 佣兵装备 | - | PET | O | [V] |
| `+0x94` | 37 | 任务卷轴 | - | QUESTSCROLL | - | [U] |

数组边界实测：前 8 个 DWORD 全 0、`idx 38` 起是垃圾 ⇒ **长度正好 38**。

**关联聚合位**：

| 项 | DLL+偏移 | 绝对地址 | 值语义 | 状态 |
|---|---|---|---|---|
| 左右开聚合位 | `D2CLIENT+0x11C414` | `0x6FBCC414` | `0`无 / `1`右开 / `2`左开 / `3`左右同时 | [V] |
| 仓库/盒子聚合位 | `D2CLIENT+0x11BC34` | `0x6FBCBC34` | `12`=仓库 / `14`=盒子（**未实测**，仅原样显示） | [?] |

⚠️ 面板打开**不阻断**任何解析（2026-09-20 起）：面板名只作提示打印。

---

## 4. 玩家 / 世界 / 房间 / 背包链

| 项 | DLL+偏移 | 绝对地址 | 含义 | 状态 |
|---|---|---|---|---|
| PlayerUnit | `D2CLIENT+0x11BBFC` | `0x6FBCBBFC` | 玩家 `UnitAny*` | [V] |
| DrlgAct | `D2CLIENT+0x11C3B8` | `0x6FBCC3B8` | 当前 Act 的 DrlgAct | [V] |
| GameInfo | `D2CLIENT+0x11B980` | `0x6FBCB980` | 游戏信息结构（名称/服务器/角色名…） | [V] |
| RosterUnitList | `D2CLIENT+0x11BC14` | `0x6FBCBC14` | 组队/玩家列表 | [存档] |
| PetUnitList | `D2CLIENT+0x11C4D4` | `0x6FBCC4D4` | 佣兵/召唤物列表 | [存档] |
| AutomapOn | `D2CLIENT+0xFADA8` | `0x6FBAADA8` | 小地图开关（= UIVar idx10 同一变量） | [V] |
| MousePos | `D2CLIENT+0x11B824` | `0x6FBCB824` | 鼠标位置（`D2_POINT_REV`） | [存档] |
| CursorInvGridX/Y | `D2CLIENT+0xE0EB8` / `+0xE0EBC` | … | 鼠标所在背包格坐标 | [存档] |

**地面物品枚举链（不依赖悬停）**：

```
pPlayer     = u32(D2CLIENT + 0x11BBFC)      PlayerUnit
pPath       = u32(pPlayer + 0x2C)           DynamicPath
pRoom1      = u32(pPath   + 0x1C)           ★入口（不是 pPlayer+0x1C！）
paRoomsNear = u32(pRoom1  + 0x00)           ★二级指针：room[i] = u32(pa + i*4)
dwRoomsNear = u32(pRoom1  + 0x24)           房间数（hackmap 注 //+04 是笔误）
pUnitFirst  = u32(room    + 0x74)           房间内第一个单位（头文件未标）
链表        沿 UnitAny.pListNext (+0xE8)
判定        dwUnitType==4(ITEM) && u8(pUnitData + 0x69)==0   （nLocation=0 躺在地上）
```

| 项 | 值 | 状态 |
|---|---|---|
| PATH_ROOM1_OFF | `+0x1C` | [V] |
| ROOM1_PTR_OFF | `+0x00`（二级指针） | [V] |
| ROOMS_NEAR_COUNT_OFF | `+0x24` | [V] |
| ROOM_UNIT_FIRST_OFF | `+0x74` | [V] |
| ITEM_LOCATION_OFF | `+0x69` | [V] |
| UNIT_NEXT_OFFSET | `+0xE8` | [V] |

**物品链（背包/装备）**：

```
玩家 UnitAny +0x60 (pInventory) → UnitInventory
  pFirstItem (UnitInventory+0x0C) → 沿 ItemData+0x64 (pNextInvItem) 走
  pInvInfo   (UnitInventory+0x14) → 已装备物品链表
镶入物：父物品 UnitAny+0x60 → pFirstItem(+0x0C) → 沿 +0x64
```

⚠️ 两个坑：① `UnitAny+0xE8` 对**背包物品/镶入物恒为 0**（会静默少报，不报错）⇒ 物品链一律 `ItemData+0x64` 优先、`+0xE8` 兜底；
② **同一指针 = 同一物品**（双手武器占两槽、2×2 物品占 4 格）⇒ 枚举必须按指针去重。

---

## 5. 内存表基址（显示名 / 词缀 / 属性都从这些表读）

### 5.1 通用表头

| 项 | DLL+偏移 | 绝对地址 | 读法 | 状态 |
|---|---|---|---|---|
| DataTables | `D2COMMON+0x99E1C` | `0x6FDE9E1C` | `DT = *(D2COMMON+0x99E1C)`，是**指针** | [V] |
| ItemStatCost | `DT+0xBCC` | — | `ISC = *(DT+0xBCC)`，记录 0x144 | [V] |
| gpt 描述区 | `D2COMMON+0x9FB94` | `0x6FDEFB94` | **基址本身**（不解引用） | [V] |
| WeaponsTxts / ArmorTxts | `D2COMMON+0x9FBA0` / `+0x9FBA8` | = gpt+0x0C / +0x14 | ★**是行数 306 / 202，不是指针**（详见新文档 §4） | [V] |
| RuneWords / RuneWordTxt | `D2COMMON+0x9FBD4` / `+0x9FBD8` | = gpt+0x40 / +0x44 | `RuneWords`=nGems(计数) / `RuneWordTxt` 才是符文表指针 | [V] |

### 5.2 gpt 描述区槽位（1.13c，逐槽实机读出）

```
gpt = D2COMMON + 0x9FB94
```

| 槽 | 实测值 | 判读 |
|---|---|---|
| `+0x00` | 716 | nItemsTxt（与 item.txt 行数吻合） |
| `+0x08/0x10/0x18` | 3 个 ptr | ItemTxt 分块基址 |
| `+0x0C/0x14/0x1C` | 306/202/208 | 分块行数（合计 = 716） |
| `+0x20` | ptr + 1452 | 递增 WORD 序号表，**未辨明** |
| `+0x28` | ptr | ★2026-09-20 复测 **== `+0x2C`**（同指 0x90 块基址）；早期在非游戏态读到 0，旧记「空槽」已更正 |
| `+0x2C` | ptr | **0x90 块基址 B**（词缀主表） |
| `+0x30` | ptr = B+747 行 | 前缀区起点 |
| `+0x34` | ptr = B+1416 行 | 第三段起点（推断 automagic） |
| `+0x38` | 71 | 第三段行数 |
| `+0x3C` / `+0x40` | ptr / 169 | 宝石表 / nGems |
| `+0x44` | ptr | Runes / Runewords 表 |
| `+0x48` | 201 | 实测 = **稀有名字词块总行数**（155 + 46），**不是**符文数 ⇒ 代码里 `GPT_N_RUNES` 这个常量名与实测含义不符，**存疑** |
| `+0x50` | ptr | **0x48 块基址 RS**（稀有物品名字词表） |
| `+0x54` | ptr = RS+155 行 | 稀有前缀名字词起点 |

**0x90 块分段**（行距 `0x90`，只有一个基址 `B = *(gpt+0x2C)`）：

| 值 | 行 | 区段 | 首行示例 |
|---|---|---|---|
| 1..747 | 0..746 | 后缀区 | `of Health` … `of the Vampire` |
| 748..1416 | 747..1415 | 前缀区 | `Sturdy` … `Cruel` |
| 1417..1487 | 1416..1486 | 第三段（**推断=自动前缀 automagic**） | `Fletcher's` … |

**0x48 块分段**（行距 `0x48`，基址 `RS = *(gpt+0x50)`）：

| 值 | 行 | 区段 |
|---|---|---|
| 1..155 | 0..154 | 后缀词（`bite` / `spiral` … `flange`） |
| 156..201 | 155..200 | 前缀词（`Beast` / `Eagle` … `Corruption`） |

取行公式（★游戏权威算法，实机反汇编）：

```
稀有取行  D2COMMON+0x71790 : lea eax,[ecx+eax*8-0x48]  →  rec = 表址 + (值-1)*0x48
词缀取行  D2COMMON+0x71841 : lea eax,[eax+ecx-0x90]    →  rec = 表址 + (值-1)*0x90
```

⇒ 两条都是 **1 基（值 − 1）**，`值 = 1` 命中表首行，`id = 0` = 无。
（已用稀有戒指上已 roll 出的 FCR/MF/PR/AR 四条属性反查 `nMin/nMax` 档位证实。）

⚠️ **词缀表只在游戏内可读**：离开游戏后该块被清零 ⇒ 表不可读时必须**显式告警**，不得静默返回空。

### 5.3 字符串表（显示名的最终来源）

```
D2LANG 镜像内（表指针是**运行时从堆加载**的，字符串本身不在 DLL 里）：
  base  (<10000) : 表 = u32(D2LANG+0x10A64)  字符串数组 = u32(D2LANG+0x10A68)  基准 0
  mid   (>=10000): 表 = u32(D2LANG+0x10A80)  字符串数组 = u32(D2LANG+0x10A6C)  基准 10000
  high  (>=20000): 表 = u32(D2LANG+0x10A84)  字符串数组 = u32(D2LANG+0x10A70)  基准 20000

查表（复刻 D2LANG.GetLocaleText = D2LANG+0x9450 的只读逻辑）：
  count = WORD[table + 0x02]
  slot  = WORD[table + 0x15 + (id - 基准)*2]     ← id → 槽位映射数组
  pStr  = DWORD[strings + slot*4]                ← 直接是 wchar_t*
```

⚠️ 字符串在**堆**（实测命中 `0x11EFxxxx` 一类地址）⇒ 服务端/mods 会改写，**必须内存查**，读本地 `.tbl` 不可靠。

---

## 6. 结构体字段偏移（监听里要用的）

### 6.1 UnitAny

| 偏移 | 字段 | 说明 |
|---|---|---|
| `+0x00` | dwUnitType | 0玩家/1怪物NPC/2物件/3导弹/4物品/5地块 |
| `+0x04` | dwTxtFileNo | 表索引（ItemTxt / MonsterTxt …） |
| `+0x0C` | dwUnitId | 单位 id（桶索引 = `& 0x7F`） |
| `+0x10` | dwMode | 模式 |
| `+0x14` | pUnitData | union：PlayerData* / ItemData* / … |
| `+0x2C` | pPath | union：StaticPath* / DynamicPath* |
| `+0x5C` | pStatList | 属性链表 |
| `+0x60` | pInventory | 自己的 inventory（孔内子物品入口） |
| `+0xE8` | pListNext | 同桶下一个单位（**物品恒 0**，别用它走物品链） |

### 6.2 ItemData

| 偏移 | 字段 | 说明 |
|---|---|---|
| `+0x00` | dwQuality | 1低质/2普通/3上等/4魔法/5套装/6稀有/7暗金/8手工 |
| `+0x0C` | dwOwnerId | 持有者 id |
| `+0x18` | dwItemFlags | `0x10`已辨识 / `0x800`已打孔 / `0x400000`无形 / `0x04000000`符文之语 |
| `+0x28` | dwFileIndex | 暗金/套装表索引（≠ 底材索引） |
| `+0x2C` | dwItemLevel | ilvl |
| `+0x30` | wItemFormat | 普通/扩展/精英 |
| `+0x32` / `+0x34` / `+0x36` | wRarePrefix / wRareSuffix / wAutoPrefix | 稀有名字词 / 自动前缀（**整块 1 基序号**，不是各表内序号） |
| `+0x38` | wMagicPrefix[3] | 魔法前缀（**符文之语物品的 [0] 是名字 locale id**） |
| `+0x3E` | wMagicSuffix[3] | 魔法后缀 |
| `+0x44` / `+0x45` | nBodyLocation / nItemLocation | 装备槽 / 位置 |
| `+0x5C` | pOwnerInventory | 持有者 inventory（未打孔时**不是**孔链） |
| `+0x64` | pNextInvItem | ★物品链的下一件（背包/孔内都靠它） |
| `+0x69` | nLocation | 0地面 / 1方块·仓库·背包 / 2腰带 / 3身上 |

### 6.3 词缀记录 / 稀有名字词记录 / ItemStatCost

| 记录 | 行距 | 字段 |
|---|---|---|
| 词缀 | `0x90` | `+0x00` szName[32] 内部名；`+0x20` WORD wLocaleTxtNo 显示名；`+0x24` nGroup（同组=同词缀不同档）；`+0x2C` nMin；`+0x30` nMax |
| 稀有名字词 | `0x48` | `+0x0C` WORD wLocaleTxtNo；`+0x26` szName |
| ItemStatCost | `0x144` | `+0x00` dwStatNo；`+0x36` nDescFunc；`+0x37` nDescVal；`+0x38` WORD wDescStrpos |

⚠️ `ItemStatCost+0x0C/+0x10`（hackmap 标 dwDivide/dwMultiply）**在本机 1.13c 不成立**：`+0x0C` 恒 1024、`+0x10` 是零碎小整数，拿它折算会得到荒谬值。
实机悬停文本对答案（符文之语「时间」）：防御 175 / MDR 7 / 四抗 15 / 毒抗上限 5 / 经验 3 / IAS 20 / FBR 18 / FCR 30 —— **全部 raw 直读 == 显示值**（系数 1）。

### 6.4 StatList（物品属性）

```
UnitAny.pStatList = +0x5C
StatList / StatListEx (flag@+0x10 bit31 = Ex)
  +0x24  sBaseStat { pStat@+0x24, wStats@+0x28 }
  +0x48  sFullStat { pStat@+0x48, wStats@+0x4C }   （仅 Ex）
  +0x50  sModStat  { pStat@+0x50, wStats@+0x54 }   （仅 Ex）
StatEx = 8 字节 { WORD wParam; WORD wStatId; DWORD dwStatValue }
```

### 6.5 坐标路径（按类型分开，别混用）

| 类型 | 结构 | x | y |
|---|---|---|---|
| 玩家 0 / 怪物 1 / 导弹 3 | DynamicPath | WORD `+0x02` | WORD `+0x06` |
| 物件 2 / 物品 4 / 地块 5 | StaticPath | DWORD `+0x0C` | DWORD `+0x10` |

统一入口 `game.read_unit_pos(handle, pPath, unit_type)` —— **别手写偏移**（曾对物件读出 y=65535）。

### 6.6 命名链路（各类型的名字从哪来）

| 类型 | 来源 |
|---|---|
| 玩家 | `PlayerData.szName[16]` @ `pUnitData+0x00` |
| 怪物/NPC | `MonsterTxt.wLocaleTxtNo` @ `+0x06` → 字符串表 |
| 物件 | `ObjectTxt.wszName[64]` @ `+0x40`（宽字符直读） |
| 物品 | `ItemTxt`（gpt 分块）→ `+0xF4` wLocaleTxtNo → 字符串表 |
| 暗金 / 套装 | Unique / Set 表，索引 = `ItemData.dwFileIndex`，再走字符串表 |
| 符文之语名 | `ItemData+0x38[0]`（locale id）→ 字符串表 |

---

## 7. 附录：VARS 全量（68 项，含"是否被代码引用"）

<details>
<summary>展开全表</summary>

| DLL | DLL+偏移 | 绝对地址 | 名字 | 引用处 |
|---|---|---|---|---|
| `D2CLIENT` | `+0xDBC48` | `0x6FB8BC48` | `ScreenSizeX` | 仅存档 |
| `D2CLIENT` | `+0xDBC48` | `0x6FB8BC48` | `ScreenSize` | 仅存档 |
| `D2CLIENT` | `+0xDBC4C` | `0x6FB8BC4C` | `ScreenSizeY` | 仅存档 |
| `D2CLIENT` | `+0xDBC54` | `0x6FB8BC54` | `LastMousePos` | 仅存档 |
| `D2CLIENT` | `+0xE0EB8` | `0x6FB90EB8` | `CursorInvGridX` | 仅存档 |
| `D2CLIENT` | `+0xE0EBC` | `0x6FB90EBC` | `CursorInvGridY` | 仅存档 |
| `D2CLIENT` | `+0xF16B0` | `0x6FBA16B0` | `Divisor` | 仅存档 |
| `D2CLIENT` | `+0xF8C9C` | `0x6FBA8C9C` | `ExitAppFlag` | 仅存档 |
| `D2CLIENT` | `+0xF9E14` | `0x6FBA9E14` | `InfoPositionX` | 仅存档 |
| `D2CLIENT` | `+0xFADA8` | `0x6FBAADA8` | `AutomapOn` | game.py; cli.py |
| `D2CLIENT` | `+0x10A608` | `0x6FBBA608` | `UnitTable` | game.py |
| `D2CLIENT` | `+0x10B9DC` | `0x6FBBB9DC` | `MapShakeY` | 仅存档 |
| `D2CLIENT` | `+0x119638` | `0x6FBC9638` | `HoverUnitId` | game.py; cli.py |
| `D2CLIENT` | `+0x11964C` | `0x6FBC964C` | `HoverUnitType` | game.py; cli.py |
| `D2CLIENT` | `+0x11973B` | `0x6FBC973B` | `QuestData` | 仅存档 |
| `D2CLIENT` | `+0x11973F` | `0x6FBC973F` | `GameQuestData` | 仅存档 |
| `D2CLIENT` | `+0x119804` | `0x6FBC9804` | `Ping` | 仅存档 |
| `D2CLIENT` | `+0x119854` | `0x6FBC9854` | `Expansion` | 仅存档 |
| `D2CLIENT` | `+0x11B824` | `0x6FBCB824` | `MousePos` | 仅存档 |
| `D2CLIENT` | `+0x11B980` | `0x6FBCB980` | `GameInfo` | game.py; cli.py |
| `D2CLIENT` | `+0x11B9A0` | `0x6FBCB9A0` | `DrawOffset` | 仅存档 |
| `D2CLIENT` | `+0x11BBB0` | `0x6FBCBBB0` | `GoldInTranBox` | 仅存档 |
| `D2CLIENT` | `+0x11BBFC` | `0x6FBCBBFC` | `PlayerUnit` | game.py; items.py; cli.py |
| `D2CLIENT` | `+0x11BC14` | `0x6FBCBC14` | `RosterUnitList` | 仅存档 |
| `D2CLIENT` | `+0x11BC38` | `0x6FBCBC38` | `CurrentViewItem` | game.py; cli.py |
| `D2CLIENT` | `+0x11BEFC` | `0x6FBCBEFC` | `IsMapShakeOn` | 仅存档 |
| `D2CLIENT` | `+0x11BF00` | `0x6FBCBF00` | `MapShakeX` | 仅存档 |
| `D2CLIENT` | `+0x11C028` | `0x6FBCC028` | `ChatTextLength` | 仅存档 |
| `D2CLIENT` | `+0x11C1B0` | `0x6FBCC1B0` | `MinmapType` | 仅存档 |
| `D2CLIENT` | `+0x11C1C0` | `0x6FBCC1C0` | `AutomapLayerList` | 仅存档 |
| `D2CLIENT` | `+0x11C1C4` | `0x6FBCC1C4` | `AutomapLayer` | 仅存档 |
| `D2CLIENT` | `+0x11C1E8` | `0x6FBCC1E8` | `AutomapPos` | 仅存档 |
| `D2CLIENT` | `+0x11C1F8` | `0x6FBCC1F8` | `Offset` | 仅存档 |
| `D2CLIENT` | `+0x11C21C` | `0x6FBCC21C` | `InfoPositionY` | 仅存档 |
| `D2CLIENT` | `+0x11C228` | `0x6FBCC228` | `MinimapOffset` | 仅存档 |
| `D2CLIENT` | `+0x11C230` | `0x6FBCC230` | `AutoMapSize` | 仅存档 |
| `D2CLIENT` | `+0x11C2AC` | `0x6FBCC2AC` | `Fps` | 仅存档 |
| `D2CLIENT` | `+0x11C2F4` | `0x6FBCC2F4` | `SelectedUnitFlag` | game.py |
| `D2CLIENT` | `+0x11C2F8` | `0x6FBCC2F8` | `SelectedUnitFlag2` | game.py |
| `D2CLIENT` | `+0x11C390` | `0x6FBCC390` | `Difficulty` | 仅存档 |
| `D2CLIENT` | `+0x11C3A0` | `0x6FBCC3A0` | `InGame` | game.py; cli.py |
| `D2CLIENT` | `+0x11C3A0` | `0x6FBCC3A0` | `MButton` | 仅存档 |
| `D2CLIENT` | `+0x11C3B8` | `0x6FBCC3B8` | `DrlgAct` | structs.py |
| `D2CLIENT` | `+0x11C4B0` | `0x6FBCC4B0` | `ShowLifeStr` | 仅存档 |
| `D2CLIENT` | `+0x11C4B4` | `0x6FBCC4B4` | `ShowManaStr` | 仅存档 |
| `D2CLIENT` | `+0x11C4D4` | `0x6FBCC4D4` | `PetUnitList` | 仅存档 |
| `D2CLIENT` | `+0x11EC80` | `0x6FBCEC80` | `LastChatMessage` | 仅存档 |
| `D2CLIENT` | `+0x123395` | `0x6FBD3395` | `QuestPage` | 仅存档 |
| `D2COMMON` | `+0x99E1C` | `0x6FDE9E1C` | `DataTables` | itemprops.py; items.py |
| `D2COMMON` | `+0x9FBA0` | `0x6FDEFBA0` | `WeaponsTxts` | 仅存档 |
| `D2COMMON` | `+0x9FBA8` | `0x6FDEFBA8` | `ArmorTxts` | 仅存档 |
| `D2COMMON` | `+0x9FBD4` | `0x6FDEFBD4` | `RuneWords` | 仅存档 |
| `D2COMMON` | `+0x9FBD8` | `0x6FDEFBD8` | `RuneWordTxt` | 仅存档 |
| `D2GFX` | `+0x1D66C` | `0x6FA9D66C` | `WinState` | 仅存档 |
| `D2WIN` | `+0x214B0` | `0x6F9014B0` | `FocusedControl` | 仅存档 |
| `D2WIN` | `+0xC9E58` | `0x6F9A9E58` | `HoverTextBuf` | game.py |
| `D2WIN` | `+0xCA658` | `0x6F9AA658` | `HoverX` | game.py |
| `D2WIN` | `+0xCA65C` | `0x6F9AA65C` | `HoverY` | game.py |
| `D2WIN` | `+0xCA660` | `0x6F9AA660` | `HoverTran` | 仅存档 |
| `D2WIN` | `+0xCA664` | `0x6F9AA664` | `HoverFlag` | game.py; cli.py |
| `D2WIN` | `+0xCA66C` | `0x6F9AA66C` | `HoverRectL` | 仅存档 |
| `D2WIN` | `+0xCA670` | `0x6F9AA670` | `HoverRectT` | 仅存档 |
| `D2WIN` | `+0xCA674` | `0x6F9AA674` | `HoverRectR` | 仅存档 |
| `D2WIN` | `+0xCA678` | `0x6F9AA678` | `HoverRectB` | 仅存档 |
| `D2NET` | `+0xB244` | `0x6FBFB244` | `UnkNetFlag` | 仅存档 |
| `BNCLIENT` | `+0x1F64C` | `0x6FF3F64C` | `BnChatMessage` | 仅存档 |
| `D2MULTI` | `+0x19C60` | `0x6F9E9C60` | `EditboxPreferences` | 仅存档 |
| `D2MULTI` | `+0x39CC0` | `0x6FA09CC0` | `GameListControl` | 仅存档 |

</details>

> ⚠️ `InGame` 与 `MButton` 在 `VARS` 里**指向同一地址** `0x6FBCC3A0`；`ScreenSizeX` 与 `ScreenSize` 同理 —— 是参考项目里的别名，不是笔误。
> `MousePos` / `LastMousePos` / `CurrentViewItem` 在 D2CLIENT 段里**定义了两遍**（读数相同）。

---

## 8. 未辨明 / 未使用 / 已知陷阱（诚实清单）

**未辨明**

- `gpt+0x20`（ptr + 1452 个递增 WORD 序号）：内容像索引表，用途未知。
- `gpt+0x64` 起：递增小整数数组，用途未知。
- `gpt+0x48` = 201：实测等于**稀有名字词总行数**（155+46），与代码常量名 `GPT_N_RUNES`（像是"符文数"）不符 ⇒ 含义存疑，用时以实测为准。
- 0x90 块**第三段（值 1417..1487）身份 = "推断" automagic**：三条依据（与 magic 块无缝隙同属一块 / 实机 `wAutoPrefix` 取值 1417..1487 / 行名语义吻合），**未经实机最终确认**。
- `HoverRectL/T/R/B`（`+0xCA66C..678`）：实到 109/99，疑似 right/bottom 或宽高，未确认。
- `UI_STASH_FLAG (0x11BC34)` 的 12/14 语义未实测。
- `ItemStatCost` 的**真实显示系数列未定位**（`+0x0C/+0x10` 已证伪）。
- 魔法/稀有/手工品的 staffmod（法杖/魔杖/头颅职业技能）没有独立字段，只能从 StatList 看出。
- 「一股邪恶力量」是游戏对无名单位的默认名，**不是解析错误**。

**未使用（已在 VARS 里存档）**：Automap 系列、Chat、Quest、ScreenSize、MousePos、CursorInvGrid、MapShake、GoldInTranBox、ShowLife/ManaStr、Expansion/Difficulty/Fps/Ping、Roster/Pet 列表、WinState、FocusedControl、UnkNetFlag、BnChatMessage、D2MULTI 的两个控件等。要用先按 `[存档]` 对待：**未验证，需实测**。

**陷阱速查**

1. 地址一律用「真实模块基址 + 偏移」换算，别硬套 DLLBASE。
2. `enum_modules()` 必须 `LIST_MODULES_32BIT`，否则 64 位宿主只看得到 WOW64 辅助模块。
3. `+0x11C2F4/F8` 是**标记**不是指针；`+0xCA658/65C` 是**坐标**不是指针。
4. `+0xCA658`（框）vs `+0xC9E58`（文本）**只差一位十六进制**，拼错会白查一轮。
5. UI 面板数组起点 = `p − 4`，不是 `p`。
6. 坐标读法按单位类型分 DynamicPath / StaticPath 两种，别混。
7. 物品链用 `ItemData+0x64`，别用 `UnitAny+0xE8`（恒 0 且静默）。
8. 同一指针 = 同一物品 ⇒ 枚举按指针去重。
9. 词缀表离开游戏即清零 ⇒ 表不可读时**必须显式告警**。
10. 判定字段一律**完整打印**，不按条件隐藏 —— "不显示 ≠ 没读到"（曾因此白查一轮）。
