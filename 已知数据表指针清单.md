# D2H 已知数据表指针清单（.tbl / .txt，Diablo II 1.13c）

> 配套文档：《`已知内存指针清单.md`》= 全局变量 / 悬停 / UI 面板那本。
> 本册只讲**数据表**：字符串表（.tbl）与数据表（.txt）在内存里的结构与指针。
>
> 版本锁 **1.13c**；目标进程 **D2loader.exe**；**只读**（仅 `PROCESS_VM_READ`，绝不写内存 / 不调用游戏函数）。
> 代码权威（本文与代码不一致时以代码为准）：
> `d2h/acquire/lang.py`（.tbl）、`itemprops.py`（词缀 / ItemStatCost）、
> `items.py`（ItemTxt / 暗金套装名）、`names.py`（命名链路）；观测点全表见 `规范.md §15.1`。
>
> 本文所有「实测值」均来自 2026-09-20 只读探针当场读数（pid 17204，角色在游戏内），
> **不是**从 hackmap 注释抄的 —— 两者不一致的地方已单独标注。

## 0. 先分清三种"表"（混了就会读错）

| 形态 | 文件来源 | 内存里长什么样 | 本工具读法 |
|---|---|---|---|
| **字符串表 `.tbl`** | `*.tbl`（本地化文本） | D2LANG 里 **3 组** `{表头, 字符串指针数组}`；**字符串本体在堆**，不在 DLL 镜像 | `lang.LocaleText` |
| **数据表 `.txt`（描述区）** | `*.txt` | 一组**平铺的记录数组**，基址散落在 **gpt 区** 与 **DataTables** | `itemprops` / `items` |
| **Link / Bin（运行时生成）** | 无（运行时算出来） | 排序索引（`pXxxLink`）、首列快捷表（`pXxxBin`） | **不用**（我们总走 `pXxxTxt` 原文） |

⚠️ 三条铁律：
1. **名字一律内存查**，不读本地 `data/*.tbl`、`*.txt` —— 服务端下发 + mods 会改写（用户明令）。
2. **`.tbl` 与 `.txt` 是两套东西**：`.txt` 表里存的是 `wLocaleTxtNo`（一个 id），真正的显示文字要去 `.tbl` 换。
3. `pXxxTxt` 里的 `Txt` 是**表数据**；`pXxxLink` 是索引，**两者不要混用**。

---

## 1. `.tbl` 字符串表（D2LANG）

### 1.1 三组结构

hackmap 拿显示名走的是 `D2GetLocaleText(no)`（`D2LANG+0x9450`）——**那是函数调用，我们不调**，
改为只读 dump 该函数体、自己复刻查表。函数体逻辑（1.13c）：

```
if (no >= 20000)  用 组3   else if (no >= 10000)  用 组2   else  用 组1
count  = WORD[ table   + 0x02 ]          ← 表头里的条数
slot   = WORD[ table   + 0x15 + (no - 基准) * 2 ]   ← id -> 槽位
pStr   = DWORD[ strings + slot * 4 ]                ← 槽位 -> wchar_t*
```

| 组 | 表头指针槽 | 字符串数组槽 | id 基准 | 实测 count |
|---|---|---|---|---|
| base（`<10000`） | `D2LANG+0x10A64` | `D2LANG+0x10A68` | 0 | **5427** |
| mid（`>=10000`） | `D2LANG+0x10A80` | `D2LANG+0x10A6C` | 10000 | **1169** |
| high（`>=20000`） | `D2LANG+0x10A84` | `D2LANG+0x10A70` | 20000 | **2991** |

- ⚠️ 注意 mid 组：表头在 `+0x10A80`、字符串在 `+0x10A6C` —— **两个槽位的顺序与另两组相反**（不是笔误，实测如此）。
- 表头内 `count` 读 **2 字节**（`table+0x02`）；id→槽位数组从 `table+0x15` 起，每项 **2 字节**。
- `GetLocaleText` 函数入口 = `D2LANG+0x9450`（**只存档，不调用**）。
- ✔ **实测抽检**：`id=2039`→`短棍【普通】`、`id=10000`→`巴特克的猛击`、`id=2698`→`国王之杖`、
  `id=20793`→`燃烧之`、`id=20525`→`白天 Daylight`；`id=0` 是 NPC 对白长文本（**正常**，0 不是"空"）。

### 1.2 坑

- ★ **字符串不在 D2Lang 镜像里**：实际命中 `0x11EFxxxx` / `0x13xxxxxx` 这类**堆地址**（实测 mid/high 组的
  字符串数组值就是 `0x1250AAEC`、`0x13101D14`），所以**读 DLL 文件里的 `.tbl` 得不到全量**。
- 文本含**颜色控制符** `ÿcX`（`\xff` + `c` + 1 字符），显示前必须 `lang.strip_color()` 剥掉。
- 宽字符串按 UTF-16LE 读，遇 `\x00` 结束；单串上限按 256 字符（`read_wstring` 的 `maxlen`）。

---

## 2. `.txt` 数据表总表 `DataTables`

### 2.1 入口（★是**指针**，必须解引用一次）

```
DT = *( D2COMMON + (0x6FDE9E1C − DLLBASE_D2COMMON) )       # 1.13c 绝对地址 0x6FDE9E1C
```
实测：槽位地址 `0x6FDE9E1C`（未重定位）→ `DT = 0x6FDEFED8`。

### 2.2 全量槽位表（搬自 hackmap `d2structs.h` 的 `sgptDataTables` 注释 + 本机实测）

记号：`[V]` 实测非零且是堆指针；`[U]` 实测有值但**该槽位解读存疑**；`[?]` 实测为 0 / 未辨明；`—` 非表指针（索引 / 缓存 / 数组）。

| 偏移 | 名称 | 实测值 | 状态 |
|---|---|---|---|
| `+0x000` | `pPlayerClassTxt` | 0 | [?] |
| `+0x008` | `pBodyLocsTxt` | 0 | [?] |
| `+0x010` | `pStorePageTxt` | 0 | [?] |
| `+0x018` | `pElemTypesTxt` | 0 | [?] |
| `+0x020` | `pHitClassTxt` | 0 | [?] |
| `+0x028` | `pMonModeTxt` | 0 | [?] |
| `+0x030` | `pPlrModeTxt` | 0 | [?] |
| `+0x038` | `pSkillCalcTxt` | 0 | [?] |
| `+0x058` | `pMissCalcTxt` | 0 | [?] |
| `+0x06C` | `pSkillCodesBin` | `0x049128F0` | [V] 运行时生成 |
| `+0x074` | `pEventsTxt` | 0 | [?] |
| `+0x07C` | `pCompCodesTxt` | `0x049128F0` | [V] 运行时生成 |
| `+0x088` | `pMonAiTxt` | 0 | [?] |
| `+0x094` | `pItemLink` | `0x126303F4` | [V] 索引 |
| `+0x0A4` | `pPropertiesTxt` | `0x0257A4A8`（n=268） | [V] |
| `+0x0B0` | `pRunesLink` | `0x126302B4` | [V] 索引 |
| `+0x0B4` | `pHireDescTxt` | 0 | [?] |
| `+0x0BC` | `pStatesTxt` | `0x13094EA0`（n=185） | [V] |
| `+0x194` | `pSoundCodesBin` | 0 | [?] |
| `+0x1A0` | `pHirelingsTxt` | `0x124E6940`（n=120） | [V] |
| `+0x9A8` | `pNpcsTxt` | `0x124EEFB0`（n=17） | [V] |
| `+0x9B0` | `pColorsTxt` | 0 | [?] |
| `+0x9BC` | `pTreasureClassExTxt` | `0x1244D934`（n=1069） | [V] |
| `+0xA78` | `pMonStatsTxt` | `0x12490A88`（n=818） | [V] |
| `+0xA84` | `pMonSoundsTxt` | `0x1243E1A0`（n=141） | [V] |
| `+0xA90` | `pMonStats2Txt` | `0x12459188`（n=609） | [V] |
| `+0xA9C` | `pMonPlaceTxt` | 0 | [?] |
| `+0xAA8` | `pMonPresetTxt` | `0x124397C0` | [V] |
| `+0xAD4` | `pSuperUniquesTxt` | `0x124E5888`（n=66） | [V] |
| `+0xB64` | `pMissilesTxt` | `0x0492F8E0`（n=945） | [V] |
| `+0xB70` | `pMonLvlTxt` | `0x123C4AE0`（n=111） | [V] |
| `+0xB78` | `pMonSeqTxt` | `0x12443658`（n=1010） | [V] |
| `+0xB8C` | `pSkillDescTxt` | `0x12515EB0`（n=245） | [V] |
| `+0xB98` | `pSkillsTxt` | `0x12ED0E98`（n=514） | [V] |
| `+0xBB8` | `pOverlayTxt` | `0x126305F4`（n=294） | [V] |
| `+0xBC4` | `pCharStatsTxt` | `0x04990A78`（n=7） | [V] |
| **`+0xBCC`** | **`pItemStatCostTxt`** | `0x04912E10`（**n=360**） | **[V] ★属性名表** |
| `+0xBE0` | `pMonEquipTxt` | `0x124EF810`（n=45） | [V] |
| `+0xBE8` | `pPetTypesTxt` | `0x02578FD8`（n=20） | [V] |
| `+0xBF8` | `pItemTypesTxt` | `0x13104C00`（n=104） | [V] |
| `+0xC0C` | `pSetsTxt` | `0x12F5E308`（n=32） | [V] 套装 |
| **`+0xC18`** | **`pSetItemsTxt`** | `0x12FAFE78`（**n=127**） | **[V] ★套装件名** |
| **`+0xC24`** | **`pUniqueItemsTxt`** | `0x12F1D0D0`（**n=538**） | **[V] ★暗金名** |
| `+0xC30` | `pMonPropTxt` | `0x12487170`（n=123） | [V] |
| `+0xC3C` | `pMonTypeTxt` | `0x1309E010`（n=59） | [V] |
| `+0xC50` | `pMonUModTxt` | `0x12438F10`（n=43） | [V] |
| `+0xC58` | `pLevelsTxt` | `0x124F0050`（n=178） | [V] |
| `+0xC64` | `pLvlPrestTxt` | `0x126491D8`（n=1132） | [V] |
| `+0xC74` | `pAnimTables` | `0x123D309C` | [V] |
| `+0xC78` | `pExperienceTxt` | `0x123C3388` | [V] |
| `+0xC7C` | `pDifficultyLevelsTxt` | `0x126D59F8`（n=3） | [V] |
| `+0xCB8` | `pCharTemplateTxt` | **0x00000017 = 23** | [U] **不是指针**，疑槽位不符 |
| `+0xCC0` | `pArenaTxt` | **0x0000023D = 573** | [U] **不是指针**，疑槽位不符 |
| `+0xCC4` | `pLvlTypesTxt` | `0x12741D88`（n 读 0） | [V] |
| `+0xCD4` / `+0xCDC` / `+0xCE4` / `+0xD04` | `pLvlWarpTxt` / `pLvlMazeTxt` / `pLvlSubTxt` / `pCubeMainTxt` | 0 | [?] 按需时才加载 |

（实测非零 38 / 58；`[?]` 的那些多为「进入对应场景后才加载」或本版本槽位与 hackmap 注释有偏移，
**用时必须先实测**，不要照名字硬套。）

### 2.3 记录（record）大小速查

| 表 | 记录 | 关键字段 |
|---|---|---|
| `ItemStatCostTxt` | `0x144` | `+0x00 dwStatNo`、`+0x36 nDescFunc`、`+0x37 nDescVal`、`+0x38 wDescStrpos`（描述模板 locale id） |
| `UniqueItemsTxt` | `0x14C` | `wLocaleTxtNo @ +0x22` |
| `SetItemsTxt` | `0x1B8` | `wLocaleTxtNo @ +0x24` |

取行：`rec = *(DT + 槽位) + idx * 记录大小`；`idx < n`（n 在同 +4 的槽位）。

---

## 3. 物品描述区 `gpt`（物品相关表的专门区）

### 3.1 入口（★是**基址本身**，**不解引用**）

```
gpt = D2COMMON + (0x6FDEFB94 − DLLBASE_D2COMMON)
```
⚠️ 与 `DataTables` 的区别：`DataTables` 要解引用一次（`DT = *(槽)`），`gpt` **直接就是基址**。
早期曾误把 gpt 解引用 ⇒ `self.gpt = 0x2CC`，**所有词缀表静默取空**（不报错）。

### 3.2 槽位表（2026-09-20 重新实测）

| 槽 | 实测值 | 判读 |
|---|---|---|
| `+0x00` | `0x2CC` = **716** | nItemsTxt（与 item.txt 行数吻合） |
| `+0x08` / `+0x0C` | ptr / **306** | ItemTxt 分块 0 的 `{基址, 行数}` |
| `+0x10` / `+0x14` | ptr / **202** | ItemTxt 分块 1 |
| `+0x18` / `+0x1C` | ptr / **208** | ItemTxt 分块 2（306+202+208 = 716 ✔） |
| `+0x20` / `+0x24` | ptr / **1452** | 递增 WORD 序号表，**未辨明** |
| **`+0x28`** | `0x1238DE9C` | ★**实测 == `+0x2C`**（同指 0x90 块基址）。**早期记为 0，已更正** |
| **`+0x2C`** | `0x1238DE9C` | ★ **0x90 块基址 B**（魔法词缀主表） |
| `+0x30` | `0x123A82CC` | = B + 747 行（实测 `(值−B)/0x90 = 747.0`） |
| `+0x34` | `0x123BFB1C` | = B + 1416 行（实测 1416.0） |
| `+0x38` | `0x47` = **71** | 第三段行数 |
| `+0x3C` / `+0x40` | ptr / **169** | 宝石表 / nGems |
| `+0x44` | `0x04991FF0` | Runes / Runewords 表（**与 `RuneWordTxt` 同一个值**，见 §4） |
| `+0x48` | `0xC9` = **201** | 实测 = 稀有名字词块总行数（155+46）；代码里常量名却是 `GPT_N_RUNES` ⇒ **名不副实** |
| `+0x50` | `0x12F53A24` | **0x48 块基址 RS**（稀有物品名字词表） |
| `+0x54` | `0x12F565BC` | = RS + 155 行 |

### 3.3 ItemTxt 分块：★已验证「三块内存连续」

| 块 | 基址 | 行数 | 区间（含） | 首行 code | 末行 code |
|---|---|---|---|---|---|
| 0 | `0x12F639AC` | 306 | `0x12F639AC`..`0x12F8347C` | `hax` | `amf` |
| 1 | `0x12F8347C` | 202 | `0x12F8347C`..`0x12F9830C` | `cap` | `nef` |
| 2 | `0x12F9830C` | 208 | `0x12F9830C`..`0x12FADB8C` | `elx` | `pr57` |

✔ **chunk0 末地址 == chunk1 首地址 == chunk1 基址**，chunk1 末 == chunk2 基址 ⇒ 三块在堆上**首尾相接**，
因此 `rec = 分块0基址 + idx * 0x1A8` 这套**线性索引对 0..715 全区间成立**（已逐点核对
`idx = 0/305/306/307/507/508/715` 两种取法结果完全一致；背包里 77 件 `txt>=300` 的物品也全部吻合）。

> 这条是**专门验证过的"怀疑排除了"**：分块只是加载器分批分配，恰好连续 ——
> 以后**不要**再因为"分 3 块"去重写取行逻辑。

ItemTxt 记录 `rec = 0x1A8`：`+0x80` 起 4 字节 = 物品代码（ascii，如 `cm3`）、
`+0xF4` WORD = `wLocaleTxtNo`、`+0x11E` BYTE = `nType`、`+0x138` BYTE = `nSocket`。

### 3.4 0x90 块分段（逐行实机核出）

行距 `0x90`，唯一基址 `B = *(gpt+0x2C)`；词缀记录：`szName[32]@+0x00`、`wLocaleTxtNo@+0x20`、
`nGroup@+0x24`、`nMin@+0x2C`、`nMax@+0x30`。

| 值 | 行 | 区段 | 实测首/末行 |
|---|---|---|---|
| 1..747 | 0..746 | **后缀区** | 行0 `of Health`(grp3) … 行746 `of the Vampire`(grp65) |
| 748..1416 | 747..1415 | **前缀区** | 行747 **空行** ；行748 `Sturdy`(grp5) … 行1415 `Cruel`(grp29) |
| 1417..1487 | 1416..1486 | 第三段（**推断 = 自动前缀 automagic**） | 行1416 `Fletcher's`(grp124) … 行1486 空 |

取行公式（★老大实机反汇编，游戏权威算法）：`rec = B + (值−1) * 0x90` —— **1 基**。

### 3.5 0x48 块（稀有物品**名字词**表）

行距 `0x48`，基址 `RS = *(gpt+0x50)`；记录：`wLocaleTxtNo@+0x0C`（WORD）、`szName@+0x26`。

| 值 | 行 | 区段 |
|---|---|---|
| 1..155 | 0..154 | 后缀词（`bite` … `flange`） |
| 156..201 | 155..200 | 前缀词（`Beast` … `Corruption`） |

取行公式：`rec = RS + (值−1) * 0x48`；实机 `wRarePrefix`(159..189) / `wRareSuffix`(64..135) 全部落在各自区段
⇒ 这两个字段是**整块 1 基序号**（名字词索引），**不是** affix 索引。

---

## 4. 单表全局（D2COMMON VARS）★与 gpt 的对应关系

`offsets.VARS["D2COMMON"]` 里那 5 个「表指针」，其实**全都落在 gpt 描述区内部**（实测交叉验证）：

| hackmap 名 | 槽位（绝对） | = gpt + | 实测值 | **真实含义**（实测） |
|---|---|---|---|---|
| `WeaponsTxts` | `0x6FDEFBA0` | `+0x0C` | **306** | ItemTxt 分块0的**行数**（**不是指针！**） |
| `ArmorTxts` | `0x6FDEFBA8` | `+0x14` | **202** | ItemTxt 分块1的行数（同上） |
| `DataTables` | `0x6FDE9E1C` | — | `0x6FDEFED8` | **真·DataTables 指针**（gpt 之外） |
| `RuneWords` | `0x6FDEFBD4` | `+0x40` | **169** | = nGems（数值，**不是指针**） |
| `RuneWordTxt` | `0x6FDEFBD8` | `+0x44` | `0x04991FF0` | 符文/符文之语表指针（**这个才是指针**） |

⚠️ **教训**：hackmap 给这几个起的名字（`...Txts` / `RuneWords`）极具误导性 —— 其中**两个是计数、不是指针**。
若照名字当指针解引用，会读到 `306`/`202` 这种小整数地址 ⇒ 必然读失败或读到垃圾。
本工具目前**没有**在任何地方用它们（只用 `DataTables`），保持这样。

---

## 5. 命名链路（从 UnitAny 走到表）

| UnitType | 中间结构 | 表指针 | 取名字段 | 再走 |
|---|---|---|---|---|
| 0 玩家 | `UnitAny+0x14 pUnitData` → `PlayerData` | — | `szName[16]@+0x00` | ascii 直读 |
| 1 怪物/NPC | `pUnitData` → `MonsterData` | `pMonsterTxt @ +0x00` | `wLocaleTxtNo @ +0x06` | → `.tbl` |
| 2 物件 | `pUnitData` → `ObjectData` | `pObjectTxt @ +0x00` | `wszName[64]@+0x00`(字符) / `szName@+0x40`(内部名) | 宽字符直读 |
| 4 物品 | `UnitAny.dwTxtFileNo` | ItemTxt 分块（gpt） | `wLocaleTxtNo @ +0xF4` | → `.tbl` |
| 5 套装 / 7 暗金 | `ItemData.dwFileIndex @ +0x28` | `*(DT+0xC18)` / `*(DT+0xC24)` | `wLocaleTxtNo @ +0x24` / `+0x22` | → `.tbl` |

- `UnitAny+0x04 = dwTxtFileNo`（**不是**指针，是索引）；`UnitAny+0x14 = pUnitData`。
- 用哪张表由 `ItemData.dwQuality` 决定（`5`=套装 / `7`=暗金），**暗金/套装的 ItemTxt 名只是"底材名"**。
- ⚠️ `MonsterTxt` 记录大小 `0x1A8`；`ObjectTxt` 记录大小 `0x1C0`（表里 `wszName @ +0x40`）。

---

## 6. 实测交叉验证与坑速查

**已验证的自洽点**
- `ItemTxt`：函数体提取的 `ptr/count` == gpt 分块（`0x12F639AC` / `716 == 306+202+208`）。
- `DataTables` 槽位 `+0xBCC`（n=360）用于读 `ItemStatCostTxt`，desc 名列与实机悬停文本一致。
- `RuneWordTxt`（`D2COMMON+0x9FBD8`）== `gpt+0x44`，两条路取到同一个符文表指针。
- `.tbl` 三组 count 合计 9587，覆盖 id 0..22990 的各段。

**坑**
1. ★ `DataTables` 是**指针**（解引用一次），`gpt` 是**基址本身**（不解引用）—— 搞反过一次，症状是"所有表静默取空"。
2. ★ **词缀表只在游戏内可读**：离开游戏后该块被清零 ⇒ 必须显式告警，**绝不静默返回空**。
3. ★ `+0x11C2F4/F8` 那类"看着像指针"的全局见《已知内存指针清单.md》§8，本册不重复。
4. `DataTables` 里 `+0xCB8` / `+0xCC0` 实测是**小整数**（23 / 573），按 hackmap 名当指针用会错 —— 用时先实测。
5. `.tbl` 的 **mid 组**表头/串槽顺序与另两组相反（`+0x10A80` 表头、`+0x10A6C` 串）。
6. `id=0` 的字符串是 NPC 对白（长文本），**不是"空"**；判空要看 `group` 是否存在，别用 `id==0`。
7. 记录大小必须精确：`ItemStatCostTxt=0x144`、`UniqueItemsTxt=0x14C`、`SetItemsTxt=0x1B8`、
   `ItemTxt=0x1A8`、`MonsterTxt=0x1A8`、`ObjectTxt=0x1C0`、词缀 `=0x90`、稀有名字词 `=0x48`。

---

## 附：本册涉及的文件

| 文件 | 内容 |
|---|---|
| `d2h/acquire/lang.py` | `.tbl` 三组定位 + 查表（复刻 `GetLocaleText`） |
| `d2h/acquire/items.py` | `ItemTextTable`（ItemTxt 定位 / 分块）、`QualityNameTables`（暗金套装） |
| `d2h/acquire/itemprops.py` | gpt 槽位常量、0x90 / 0x48 块、`ItemStatCost`、词缀取行 |
| `d2h/acquire/names.py` | 各 UnitType 的命名链路 |
| `d2h/acquire/structs.py` | `MonsterTxt` / `ObjectTxt` / `ItemData` 字段偏移 |
| `规范.md §15.1 I` | 词缀表 / 属性表的完整观测结论 |
